# 2026-09-14 · Higher Foundation 2.0 收口

Timestamp Source: SYSTEM (2026-09-14T23:51:17+0800)

## 任务
执行 `HIGHER_FOUNDATION_2_CLOSURE_MASTER_TASK.md`（Foundation 2.0 MAIN-ONLY 一次性收口，Phase A→N）。

## 起点确认（§0 预检）
- Repository: https://github.com/moore-hy/Higher
- branch = main，working tree = clean
- HEAD = 0aa69b3e7125aed99f5e21857baf34c0cbdf65fc（== origin/main，与任务书基线一致）
- 本地额外分支：android/dev、integrate/higher-1.0.0、refactor-foundation-2.0（均已并入 main，diff 为空）、release/1.0.0

## Phase A Baseline Gate 结果
| Gate | 结果 |
|---|---|
| BASELINE_TSC (`npx tsc --noEmit`) | PASS |
| BASELINE_FRONTEND_BUILD (`npm run build`) | PASS (7.12s) |
| BASELINE_GENERATED_TYPES (`git diff --exit-code src/generated`) | PASS（内容幂等，exit 0） |
| BASELINE_CARGO_FMT (`cargo fmt --check`) | **FAIL**（201 文件差异，仓库为 2 空格非 rustfmt 风格，无 rustfmt.toml，fmt 不在任何 CI） |
| BASELINE_CARGO_CHECK (`cargo check -j 1`) | PASS (6m08s, 34 warnings) |
| BASELINE_IPC_TEST (`cargo test -j 1 ipc::dto`) | PASS（2 passed；测试 exe 可运行，SAC 未拦截） |
| `npm ci` | 卡死 30min（`idealTree buildDeps` 后无进展），kill 后改用 `npm install` 恢复 |

## 重要环境事实
- **`npm ci` 在本沙盒卡死**（cache 与文件计数静止）。改用 `npm install --no-audit --no-fund --prefer-offline` 可推进（但也偏慢）。
- **修复了真实的基线缺口**：`@tanstack/react-query@5.102.8` 与 `temporal-polyfill@1.0.5` 原仅在 package.json/lock 中声明但从未安装（`npm ls` 报 UNMET）。本次已真正安装，Phase B 可用。
- **`.git/refs/remotes/**` 被沙盒禁止写入**（refs/heads、objects 可写）。`git fetch` 只能写 FETCH_HEAD，`origin/main` ref 无法建立 → 最终审计需用 SHA `0aa69b3` 代替 `origin/main`。
- **CRLF stat 脏**：`core.autocrlf=true` 且无 `.gitattributes`，ts-rs 生成 `src/generated/*.ts` 为 LF → 跑完 `generate:types` 后 `git status` 显示 18 个 M，但 `git diff` 为空（内容幂等）。用 `git checkout -- src/generated` 恢复。
- 沙盒 shell PATH 为 Windows 风格（分号），需在 bash 内显式重设 PATH 才能用 coreutils；PowerShell 工具不回传 stdout → 统一用 bash + 显式 PATH。

## 待 Owner 决策（阻塞 Phase B 首个 commit）
1. `cargo fmt --check` 门禁（§24/§27 要求 PASS，但仓库从未 fmt）：是否允许一次性 `cargo fmt` 全仓（201 文件）作为独立 chore commit。

## Owner 决策（已获授权）
1. `cargo fmt --check` 门禁 → **授权一次性全仓格式化**。
2. 推进方式 → **连续推进 B→N**（每 Phase 完成后本地 commit，除 Hard Stop 外不中途停顿）。

## Phase A 收口
- 执行 `cargo fmt`（默认 rustfmt，201 文件；第二次运行后 `--check` 收敛为 0 差异）。
- 格式化后 `cargo check -j 1` PASS（0 error，34 warning 不变）。
- 提交：`b8e28aa chore(rust): apply rustfmt baseline to repository`（仅 201 个 .rs，未含 .workbuddy/）。
- 至此 Phase A 全部 Gate PASS。

## Phase B 进行中（未提交，§7.8 要求 Phase 末一次性提交）
已完成：
- 新增 `src/query/client.ts`（§7.2 QueryClient 单例：query retry=1、mutation retry=0、staleTime 30s、refetchInterval 关闭）。
- 新增 `src/query/keys.ts`（§7.3 key 单一事实源，profile-scoped key 均含 profileId；含 learningItems）。
- `src/main.tsx` 挂载 `<QueryClientProvider client={queryClient}>`（§7.2）。
- 验证：`npx tsc --noEmit` PASS、`npm run build` PASS。

未完成（Phase B 剩余）：
- 其余 domain 模块（profiles/goals/tasks/sessions/planning/knowledge/data/settings/sync 的 useQuery/useMutation hooks）。
- 14 个页面 `useEffect→setLoading` 迁移（Today/Planning/Knowledge/Data/Settings/Sync/ProfileSelector/Tasks/Goals/Evaluations/History/LearningWorkspace/Progress/Review）。
- `ActiveProfileContext` 删除 refreshKey/triggerRefresh（当前 51 处引用，跨 18 文件），保留 bounded bootstrap（timeout/2 attempts/error phase）。
- `sync://completed` / `ai://applied` → 精准 invalidate。

## 规模事实（供架构方评估）
- `src/api.ts` 286 个导出 / 78KB；`src/types.ts` 33KB；Settings.tsx ~115KB；Knowledge.tsx ~80KB。
- Rust：164 个 src/*.rs + 81 个集成测试文件（src-tauri/tests/）。
- 沙盒单次重活耗时：`cargo check` 首次 6m08s、`cargo test ipc::dto` 15m46s、`npm ci` 卡死 30min。Phase A→N 全量在一个会话内不可完成。
