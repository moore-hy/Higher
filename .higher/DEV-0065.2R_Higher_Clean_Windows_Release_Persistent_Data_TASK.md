# DEV-0065.2R · Higher Clean Windows Release + Persistent Data
## Supersedes DEV-0065.2 Windows Release & Installer v1

> User = Product Owner  
> ChatGPT = Product / Architecture Decision Owner  
> Trae = Implementation Engineer
>
> This task supersedes the previous DEV-0065.2 task wherever they conflict.
>
> Core product decision changed:
>
> ```text
> DO NOT migrate any current development/test data.
> The first installed Higher must be a clean empty shell.
> Future installed Higher versions must preserve the user's production data.
> ```

---

# 1. Goal

Create the first real Windows-installable Higher.

The installed app must start clean:

```text
Higher 0.3.0
↓
first launch
↓
fresh production database
↓
no dev profiles
no dev tasks
no dev goals
no dev knowledge
no dev sessions
no dev AI conversations
no dev API settings
no dev wallpaper
```

But after the user begins using the installed Higher:

```text
Higher 0.3.0
↓
creates real user data
↓
upgrade to 0.4.0 / 0.5.0 / later
↓
same data remains
```

This is the highest-priority persistence rule.

---

# 2. Baseline

Required baseline:

```text
HEAD:
295d4e02ffb689cf77709f17522e58c321293ce1

branch:
main

worktree:
clean
```

Before editing:

```powershell
cd "C:\Users\37653\Desktop\Higher"

git rev-parse HEAD
git branch --show-current
git status --short
```

If baseline differs:

```text
STOP
BASELINE_MISMATCH
```

Do not reset / restore / clean.

---

# 3. Product Identity Is Permanent

Lock permanently:

```text
productName = Higher
identifier  = com.higher.desktop
```

This identifier is part of the production-data identity.

Future normal updates must keep:

```text
com.higher.desktop
```

unless ChatGPT explicitly designs a migration.

Do NOT rename it casually.

---

# 4. Version

Current packaged internal release:

```text
Higher 0.3.0
```

Align:

```text
tauri.conf.json = 0.3.0
package.json = 0.3.0
package-lock root = 0.3.0
Cargo.toml package.version = 0.3.0
Cargo.lock root package = 0.3.0
```

---

# 5. Main Executable

Keep Rust crate/package architecture.

Set user-facing main binary:

```text
Higher.exe
```

via Tauri `mainBinaryName`.

No user-facing `app.exe`.

---

# 6. Installer

Use:

```text
NSIS only
currentUser install
Simplified Chinese
offline WebView2 installer
```

Do not build MSI.

Do not require administrator privileges for normal installation.

Do not add:

```text
auto-start
tray
background service
auto-updater
license system
```

in this DEV.

---

# 7. Clean First Install

ABSOLUTE RULE:

The release installer must contain ZERO developer/test personal data.

Forbidden from bundle:

```text
src-tauri/.data/**
src-tauri/.webview-data/**
*.db
*.sqlite
.env
API keys
profiles
tasks
goals
knowledge
sessions
AI conversations
AI provider secrets
wallpaper blobs
```

The release app creates its own new data only after first launch.

---

# 8. REMOVE Previous Dev-Data Migration Requirement

The previous task proposed:

```text
scripts\Migrate-DevData-ToRelease.ps1
```

That requirement is CANCELLED.

Do NOT create a developer-data migration helper.

Do NOT copy:

```text
src-tauri\.data
src-tauri\.webview-data
```

into production.

The current test data is disposable.

---

# 9. Canonical Production Data Root

Windows installed Higher must persist machine-local production data at:

```text
%LOCALAPPDATA%\com.higher.desktop\
```

Use:

```rust
app.path().app_local_data_dir()
```

for production persistent app data.

Do NOT use `app_data_dir()` for Higher production DB/attachments/vault/backups.

---

# 10. Production Storage Layout

Canonical layout:

```text
%LOCALAPPDATA%\com.higher.desktop\
│
├─ higher.db
├─ attachments\
├─ vault\
├─ backups\
└─ WebView2 application storage
```

The exact WebView2 internal substructure is managed by Tauri/WebView2.

Higher must not invent another profile-specific application root.

---

# 11. Database Persistence Contract

`higher.db` is the user's permanent Higher database.

Normal software operations MUST NOT delete it:

```text
upgrade
repair/reinstall
launch
version change
program-file replacement
desktop shortcut recreation
Start Menu shortcut recreation
```

Future versions must continue opening the same database path.

---

# 12. Future Schema Update Contract

Current schema remains:

```text
v024
```

This DEV adds:

```text
0 migrations
```

For future Higher versions:

```text
existing DB
↓
backup
↓
forward migration
↓
same DB lineage
```

Never implement future upgrades as:

```text
delete old DB
create new DB
```

unless the user explicitly requests reset.

This policy should be documented in `.higher/ENVIRONMENT.md`.

---

# 13. Future Backup Rule

Do not add a new migration in this DEV.

But lock the architecture rule:

Before any future destructive/structural schema migration:

```text
backup current higher.db
```

into:

```text
%LOCALAPPDATA%\com.higher.desktop\backups\
```

with timestamp/version metadata.

This is a future contract, not a new current migration.

---

# 14. DB Path Consistency

Current release audit found path inconsistency between:

```text
lib.rs
db.rs/comments/environment
```

Fix production path truth so all agree on:

```text
%LOCALAPPDATA%\com.higher.desktop\higher.db
```

No split between Roaming and Local AppData.

---

# 15. Attachments / Vault / Backups

Production:

```text
attachments
vault
backups
```

must live beside `higher.db` under the same AppLocalData root.

They must NOT live inside:

```text
Program Files
installation folder
project directory
src-tauri
```

---

# 16. WebView Persistent Preferences

Installed Higher's WebView state should persist under the same stable Tauri application identity.

This includes frontend persistence such as:

```text
wallpaper IndexedDB
appearance preferences
AI rail expanded/collapsed
Planning view preference
```

First installed Higher begins clean.

After the user configures these in installed 0.3.0, normal upgrades should keep them.

Do NOT manually migrate current dev `.webview-data`.

---

# 17. Program Files vs User Data

Architectural separation:

```text
PROGRAM
Higher.exe
WebView assets
runtime resources
installer-managed files
```

and:

```text
USER DATA
higher.db
attachments
vault
backups
WebView storage
```

are separate.

Installer may replace PROGRAM.

Installer must preserve USER DATA.

---

# 18. Uninstall Policy

Default product behavior:

```text
Uninstall Higher
→ remove application
→ remove shortcuts
→ preserve user data
```

The default uninstall flow must not erase:

```text
%LOCALAPPDATA%\com.higher.desktop\
```

If stock Tauri NSIS displays a delete-app-data option:

```text
it must NOT be selected by default
```

If stock behavior deletes app data by default:

```text
STOP
UNINSTALL_DATA_DEFAULT_UNSAFE
```

Do not silently accept it.

---

# 19. Explicit Data Reset Is Separate

Deleting Higher itself is NOT equivalent to deleting the user's learning history.

If future product wants:

```text
Delete all Higher data
```

that must be an explicit, separate user action.

Not part of DEV-0065.2R.

---

# 20. Update/Reinstall Persistence

Human Runtime must prove:

```text
Install 0.3.0
↓
create real test production data
↓
close Higher
↓
run 0.3.0 installer again / repair/reinstall
↓
open Higher
↓
data remains
```

Later, when 0.4.0 exists, the same rule applies:

```text
0.3.0 data
→ install 0.4.0
→ data remains
```

---

# 21. Release Build

Create:

```text
scripts\Build-Higher-Release.ps1
```

It must:

```text
verify clean worktree
print HEAD
tsc
frontend build
cargo check
release tests
tauri build
locate NSIS installer
copy to release\
calculate SHA256
```

Output:

```text
release\Higher_0.3.0_Setup.exe
release\Higher_0.3.0_SHA256.txt
```

Add:

```text
release/
```

to `.gitignore`.

---

# 22. No Dev Runtime Dependency

Installed Higher must work with:

```text
Trae closed
terminal closed
npm stopped
Vite stopped
cargo stopped
project folder not running
```

The installed app embeds production frontend assets.

No `localhost` dependency.

---

# 23. Mock Safety

Packaged Tauri release must not activate browser-development mock injection.

Do not redesign mocks unless current runtime check is actually unsafe.

Automated/runtime test must confirm packaged Higher uses real Tauri APIs.

---

# 24. Current Higher UI Freeze

Do not change:

```text
custom DesktopTitlebar
global wallpaper system
Sidebar
Today
Planning
Knowledge
LearningWorkspace
Settings
Higher AI Rail
```

except release-path changes required by this task.

---

# 25. AI Runtime Freeze

No changes to:

```text
src-tauri/src/ai/**
```

No changes to:

```text
Provider
Turn Interpreter
SemanticAction
Grounding
Pending Action
Conversation
Streaming
ChangeSet
Approval
Apply
```

AI Runtime delta:

```text
0
```

---

# 26. Domain Freeze

No behavior changes to:

```text
Profiles
Goals
Tasks
Recurring Tasks
Planning
Knowledge
Sessions
Reviews
Data
```

---

# 27. Schema

Current:

```text
v024
```

After DEV:

```text
v024
```

Migration:

```text
0
```

---

# 28. Dependencies

New npm dependency:

```text
0
```

New Rust dependency:

```text
0
```

Packaging tool downloads are build tooling, not app-runtime dependencies.

---

# 29. Current Icon

For internal 0.3.0 installer, current committed icon pack may be used.

Record:

```text
P2 BRANDING
final Higher icon not yet approved
```

Do not block internal packaging.

---

# 30. Code Signing

Internal 0.3.0:

```text
unsigned
```

Do not fake/bypass Windows trust.

SmartScreen / Unknown Publisher may appear.

If Windows security blocks execution:

```text
STOP
ENV_BLOCKED_WINDOWS_SECURITY
```

Do not disable Windows protections.

---

# 31. Required Tauri Configuration

Keep:

```text
productName = Higher
identifier = com.higher.desktop
app.windows = []
```

Set:

```text
version = 0.3.0
mainBinaryName = Higher
bundle targets = nsis
NSIS currentUser
NSIS SimpChinese
WebView2 offlineInstaller
Start Menu folder = Higher
```

Do not create a second Tauri window.

---

# 32. Asset Scope

After attachments move/confirm under AppLocalData, asset protocol permissions must match that production path.

Do NOT broaden to arbitrary filesystem access.

If current Tauri scope syntax conflicts with AppLocalData:

```text
STOP
ASSET_SCOPE_CONFLICT
```

Report evidence.

---

# 33. New Test

Create:

```text
src-tauri/tests/batch0652_release.rs
```

At minimum test:

```text
R01 identity
R02 version alignment
R03 Higher.exe identity
R04 NSIS only
R05 currentUser
R06 SimpChinese
R07 offlineInstaller
R08 dynamic main window unchanged
R09 app_local_data_dir production usage
R10 DB path consistency
R11 attachments/vault/backups same root
R12 no personal test data bundled
R13 release/ gitignored
R14 AI source frozen
R15 schema v024
R16 no migration
R17 build script safety
R18 no dev-data migration script
R19 no installer copy of .data/.webview-data
R20 no new dependencies
```

---

# 34. Automated Gate

Run:

```powershell
cd "C:\Users\37653\Desktop\Higher"

npx tsc --noEmit
npm run build

cd src-tauri

cargo check -j 1

$env:RUST_TEST_THREADS='1'

cargo test --test batch0652_release -j 1
cargo test --test batch0651_ui -j 1
cargo test --test batch064r2_ui -j 1
cargo test --test batch064_ui -j 1
cargo test --test batch063_ui -j 1
cargo test --test ai_panel -j 1
cargo test --test batch062r1 -j 1
cargo test --test batch062r -j 1
cargo test --test batch062 -j 1
```

No real Provider network calls.

---

# 35. Build Installer

After automated gate:

```powershell
cd "C:\Users\37653\Desktop\Higher"

powershell -ExecutionPolicy Bypass -File ".\scripts\Build-Higher-Release.ps1"
```

Expected:

```text
release\Higher_0.3.0_Setup.exe
release\Higher_0.3.0_SHA256.txt
```

---

# 36. Human Runtime H01 — Clean Install

Before first installed-app test:

```text
do NOT copy src-tauri\.data
do NOT copy src-tauri\.webview-data
```

Install Higher.

Expected first run:

```text
no old profile
no old tasks
no old knowledge
no old AI chat
no old wallpaper
```

This is PASS.

---

# 37. H02 — Create Production Data

Inside installed Higher create a small production test set:

```text
Profile:
INSTALL-PERSIST

Task:
PERSIST-TASK

Knowledge:
PERSIST-KNOWLEDGE

optional:
set wallpaper
configure AI provider
```

This data now represents the real installed-app persistence test.

---

# 38. H03 — Restart Persistence

Close Higher completely.

Reopen from desktop shortcut.

Expected:

```text
INSTALL-PERSIST exists
PERSIST-TASK exists
PERSIST-KNOWLEDGE exists
wallpaper/preferences persist if created
```

---

# 39. H04 — DB Physical Location

Confirm:

```text
%LOCALAPPDATA%\com.higher.desktop\higher.db
```

exists.

Confirm no production DB inside:

```text
Higher installation directory
C:\Users\37653\Desktop\Higher\src-tauri\.data
```

is required by the installed app.

---

# 40. H05 — Reinstall Same Version

Close Higher.

Run:

```text
Higher_0.3.0_Setup.exe
```

again.

Perform normal reinstall/repair path.

Open Higher.

Expected:

```text
INSTALL-PERSIST still exists
PERSIST-TASK still exists
PERSIST-KNOWLEDGE still exists
```

---

# 41. H06 — Program Independence

Close:

```text
Trae
PowerShell
npm
cargo
```

Launch desktop Higher.

Expected:

```text
works normally
```

---

# 42. H07 — Functional Smoke

Installed Higher:

```text
Today
Planning Month/Week
Knowledge
Knowledge Graph
LearningWorkspace
Settings
Task CRUD
Session notes
```

works.

---

# 43. H08 — AI Smoke

If provider is configured in installed Higher:

```text
AI Chat works
real Proposal works
Apply works
```

No AI Runtime regression.

---

# 44. H09 — Uninstall Default Safety

Windows Settings:

```text
Installed Apps → Higher → Uninstall
```

Default flow must preserve user data.

After uninstall confirm:

```text
%LOCALAPPDATA%\com.higher.desktop\
```

still exists.

If default uninstall deletes it:

```text
STOP
UNINSTALL_DATA_DEFAULT_UNSAFE
```

---

# 45. H10 — Reinstall After Uninstall

Reinstall same Higher 0.3.0.

Launch.

Expected:

```text
INSTALL-PERSIST
PERSIST-TASK
PERSIST-KNOWLEDGE
```

return.

This proves:

```text
program lifecycle
≠
user-data lifecycle
```

---

# 46. H11 — Desktop / Start Menu

Verify:

```text
desktop shortcut works
Start Menu Higher works
```

No npm/Vite needed.

---

# 47. H12 — Window Shell

Installed app verifies:

```text
custom titlebar
drag
double-click maximize
minimize
maximize/restore
close
resize
AI Rail collapsed/expanded
```

---

# 48. H13 — Version

Windows Installed Apps / package metadata:

```text
Higher 0.3.0
```

---

# 49. H14 — Offline Installer

Optional recommended test:

```text
disconnect network
run installer
```

Expected:

```text
installation itself does not require WebView2 download
```

Remote AI still requires internet when used.

---

# 50. Allowed Files

Allowed:

```text
tauri.conf.json
package.json
package-lock.json
src-tauri/Cargo.toml
src-tauri/Cargo.lock
src-tauri/src/lib.rs
src-tauri/src/db.rs
src-tauri/capabilities/default.json
.gitignore

scripts/Build-Higher-Release.ps1
src-tauri/tests/batch0652_release.rs

.higher/ENVIRONMENT.md
.higher/TRAE_RUN.md
```

Minimal existing release-test adjustments are allowed only when directly contradicted by AppLocalData release-path truth.

---

# 51. Forbidden Files

Do not modify product UI/domain sources:

```text
src/App.tsx
src/Layout.tsx
src/styles.css
src/pages/**
src/components/**
src/appearance/**
src/api.ts
src/types.ts

src-tauri/src/ai/**
src-tauri/src/repository/**
src-tauri/src/migrations/**
```

If packaging requires business/UI changes:

```text
STOP
RELEASE_SOURCE_CONFLICT
```

---

# 52. Forbidden Operations

Never:

```text
git reset
git clean
git restore .
git checkout .
delete src-tauri\.data
delete user's production AppLocalData
copy developer DB into installer
copy private API keys into installer
```

---

# 53. Git

Trae:

```text
Commit: NO
Push: NO
Tag: NO
```

Human Runtime first.

---

# 54. Definition of Done

Automated:

```text
Higher 0.3.0
Higher.exe
NSIS
currentUser
SimpChinese
offlineInstaller
stable identifier
AppLocalData production root
clean first install
no developer data migration
release build script
SHA256
schema v024
migration 0
AI delta 0
domain delta 0
tests pass
installer builds
```

Human:

```text
first install clean
installed Higher standalone
new installed data persists after restart
same-version reinstall preserves data
normal uninstall preserves data
reinstall restores data
desktop shortcut works
Start Menu works
core Higher works
```

---

# 55. Final Trae Report

```text
DEV-0065.2R RELEASE RESULT

Status:
AUTOMATED GATE PASSED · INSTALLER BUILT · HUMAN RUNTIME PENDING
/ STOP <CODE>

Baseline:
HEAD:
Branch:
Worktree Before:

Product:
Higher

Version:
0.3.0

Identifier:
com.higher.desktop

Main Binary:
Higher.exe / FAIL

Bundle:
NSIS / FAIL

Install Mode:
currentUser / FAIL

Installer Language:
SimpChinese / FAIL

WebView2:
offlineInstaller / FAIL

First Install:
CLEAN / FAIL

Dev Data Migration:
NONE

Dev Personal Data Bundled:
NO / FAIL

Prod Data Root:
%LOCALAPPDATA%\com.higher.desktop / FAIL

DB:
%LOCALAPPDATA%\com.higher.desktop\higher.db / FAIL

Upgrade/Reinstall Data Policy:
PRESERVE / FAIL

Uninstall Default Data Policy:
PRESERVE / HUMAN PENDING

Schema:
v024

Migration:
0

AI Runtime Changes:
0

Domain Changes:
0

Dependencies:
0

Frontend:
tsc:
build:

Rust:
cargo check:

Tests:
batch0652_release:
batch0651_ui:
batch064r2_ui:
batch064_ui:
batch063_ui:
ai_panel:
batch062r1:
batch062r:
batch062:

Installer:
original:
release copy:
size:
sha256:

Human Runtime:
PENDING

Known P2:
Final Higher icon
Planning selected-date UX
Old Conversation no_changeset anomaly

Files Added:
Files Modified:

TRAE_RUN:
Updated

ENVIRONMENT:
Updated

Git Commit:
NO

Git Push:
NO

Git Tag:
NO
```

Then STOP.

Do not begin License work.
