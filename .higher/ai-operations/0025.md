# AI 操作记录 0025 - BATCH-03 / DEV-0025：Today Task Center V2

- 时间：2026-08-15；无 Migration

## 1. 页面重排（任务第一）

日期 → 今日任务（常驻「+ 新建任务」）→ ✨AI 今日建议（轻量卡片，不比任务重要）→ 今日概览（简要数据）

## 2. 修复严重 UX 问题（§33）

「+ 新建任务」常驻任务卡头部——无论今天 0/1/5/20 个任务始终可创建（此前创建第二个任务无入口）。

## 3. Task CRUD

- Create/Read/Update/Delete/Complete/Uncomplete 全支持
- 行内容：☐/☑ checkbox、标题、关联知识、planned_time、来源（重复任务/来自规划）、操作（开始学习/编辑/删除）
- **Checkbox 即操作**：点 ☑ 完成、再点恢复未完成（无 Modal）；乐观更新+失败回滚+错误提示
- 过滤 chips：全部 N / 未完成 N / 已完成 N（默认全部；已完成自然沉底）
- 排序：planned_time ASC（NULL 在后）→ id（后端 list_today_by_profile SQL 实现同一规则）

## 4. 新建/编辑（[TaskModal.tsx](file:///C:/Users/37653/Desktop/Higher/src/components/TaskModal.tsx)，Calendar 共用）

- 字段：标题* / 关联知识*（按现有业务规则 learning_item_id NOT NULL）/ 日期 / 时间（可选）
- 知识搜索 + **快速新建知识**（选父级→创建并立即关联；不要求退出 Today）
- 编辑改 planned_date → 立即从 Today 消失进入目标日期（Calendar 同步）

## 5. 删除保护

有 Session 的任务后端拒绝删除（人话："该任务已有 N 条学习记录，不能删除"）；普通任务确认后删除。

## 6. 后端（task.rs）

- Task +planned_time/recurring_rule_id；update（同 Goal 校验）/uncomplete/delete（Session guard）/
  create_from_rule/exists_for_rule_date；list_today_by_profile 排序升级
- 新命令：uncomplete_task / update_task / delete_task / list_tasks_by_range_by_profile
- humanize_repo_err：Repository 人话错误（不暴露 FK constraint 等技术词）

## 7. AI 兼容

list_tasks 增列 planned_time / from_recurring——AI 无需第二套 Task 概念（测试断言）。

## 8. Tests（batch03.rs）

同日连续 5 任务 / range 读取 / update 标题+日期+时间（跨日迁移）/ complete+uncomplete / delete /
Session guard 拒删 / Profile 隔离 + 排序（A 档案仅 A 任务；B 仅 B）。
