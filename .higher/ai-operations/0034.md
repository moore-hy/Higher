# AI 操作记录 0034 - BATCH-03.1 / DEV-0034：Knowledge Workspace + Tree + Graph UX Repair

- 时间：2026-08-15；无 Migration（仍 learning_items.id + parent_id；无 graph 表）

## 1. 树 = 主结构编辑入口（§65）

左侧树已有：+ 新建知识（Goal 存在即可建 Root，不要求 Stage/Plan；§66）、节点 ··· 菜单
（新建子知识/重命名/删除）。本 DEV **补「移动到…」**：Knowledge 选择器（排除自身+后代+跨 Goal；
含"作为顶级知识"）→ 正式 moveLearningItem。

## 2. 快速新建 Knowledge（§67）

树底 + 树菜单的行内输入（名称 Enter 即建）；TaskModal/开始学习弹窗内也各有快速新建（名称+父级可选）。

## 3. Workspace 内容优先（§68-73；BATCH-03 已达成，保持）

Header（标题/Path/状态小字+统计小字）→ **我的知识**（主编辑区）→ 学习记录 → 知识附件 →
学习证据（details 折叠：验证+问题）。标题右侧：开始学习 / ✨AI / 记录验证（保留）。

## 4. Graph 重做（§74-83；去 Debug 感）

- **去掉顶部横向 CRUD 工具条**；顶部只保留：`知识图 · [+ 新建知识] · [适应画布(N%)]`
- 节点卡片：名称 + 状态**小字**（+子数）+ ⋯；当前/选中节点**蓝框**，其余普通边框（不再多色）
- **单击选中 → 轻量浮层**：`名称 · N 个子知识 · [打开] [新增子知识] [⋯]`
- ⋯ 菜单（浮层/节点均可）：打开/新增子知识/重命名/移动到…/删除
- **Graph 可建 Root**（§79：+ 新建知识 → onCreateRoot 正式 createRootLearningItem）
- 布局（§82）：父上子下统一间距（ROW_H 78 / COL_W 200）、叶子节点顺序分配 x 不重叠、
  按节点数自动扩画布、外层容器滚动 + 「适应画布」zoom（viewBox 缩放 1→1.5 循环）
- 双击节点 = 打开（回工作区定位）；与左侧树同一 items props → 任何修改两侧即时同步（§81）

## 5. 删除语义（§84-85）

正式 safe delete（child/Task/Session/Evaluation/Attachment Guard 人话拒绝）；未实现危险级联硬删除。

## 6. Tests

batch03 已覆盖：child/rename/move 根↔子/self/后代/跨 Goal/跨 Profile 拒/有子 delete guard/同源。
