# AI 操作记录 0018 - DEV-0018：Knowledge Media + Dual View（BATCH-02 3/6）

## 1. Migration v009（唯一业务 Migration，禁 v010）

learning_attachments：learning_item_id NOT NULL（FK CASCADE）/ session_id（FK SET NULL）/
attachment_type / file_name / relative_path / mime_type / caption / created_at + 2 索引。
幂等；v008 旧数据保留（测试覆盖）。**实机 `database now at v009`。**

## 2. 文件存储

- relative_path = <profile>/<goal>/<item>/<纳秒uuid>.<ext>（禁绝对路径 / 统一 / 分隔）
- 根目录：dev = src-tauri/.data/attachments（与 DB 同一 dev 约定，沙箱安全——
  实测 AppData 在当前受限环境被拒 os error 5，已按项目既有 dev 约定回落并记录）；
  prod = AppData/attachments
- safe_delete 增加附件计数检查（含附件节点先删附件，防孤儿文件——明确策略）

## 3. 附件能力

- 上传图片/视频：Tauri 官方 dialog 插件选路径 → Rust 复制（无 base64 大文件 IPC）；
  mime 按扩展映射
- 画图：原生 Canvas（画笔/橡皮/颜色/撤销20步/清空/保存 PNG）→ base64 保存为 drawing
- 图片缩略（read_attachment_image base64）+ 点击原图 + 系统程序打开；
  视频文件卡片 + [打开]（系统播放器，无转码/抽帧）
- 删除：确认后 DB 记录 + 本地文件一并删除
- 后端 Guardrail：跨 Profile item 拒绝 / session 与 item 不一致拒绝（测试覆盖）

## 4. 入口

- Learning Workspace：笔记区 + 附件区（上传图片/上传视频/画图，关联当前 Session+Item）
- Knowledge：独立「知识附件」区（session_id=NULL，不要求先开始 Session）+
  学习记录展开内嵌该 Session 附件（只读）

## 5. Knowledge 双视图

- 顶部 [工作区][知识图] 切换，默认工作区（原目录树+编辑器完整保留）
- KnowledgeGraph：React+SVG 树状可视化（leaf 序布局 + 贝塞尔连线；节点/折叠/点击回工作区打开；
  掌握状态描边着色；横向滚动；无图谱库/物理引擎/拖拽/动画/3D）

## 6. 测试 attachments.rs（7 用例）

v009 应用+幂等 / v008→v009 旧数据 / create+relative_path 规范 / 跨 Profile 拒绝 /
session-item 不一致拒绝 / delete metadata 幂等 / safe_delete 附件阻止。
