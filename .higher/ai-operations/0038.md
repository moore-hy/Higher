# AI 操作记录 0038 - BATCH-03.2：UX Convergence Rebuild（DEV-0301~0309）

- 时间：2026-08-15；**Migration v012**

## 1. v012（[v012_ux_convergence.rs](file:///C:/Users/37653/Desktop/Higher/src-tauri/src/migrations/v012_ux_convergence.rs)）

任务书 §六.2 授权的迁移（先学再归档 + 树拖拽排序）：
1. `study_sessions` 重建：`learning_item_id → 可空`（快速学习）+ 新增 `goal_id NOT NULL`（Profile 直属）；INSERT SELECT 保 id 零破坏
2. `learning_attachments` 重建：`learning_item_id → 可空`（快速学习的粘贴/上传媒体先挂 Session；与 v009 原表同构仅放开可空）
3. `learning_items` 新增 `sort_order INTEGER NOT NULL DEFAULT 0`（手动排序；默认回退 id 序）
实机 `latest v012` 升级成功，真实 DB 172KB 旧数据保留。

## 2. DEV-0301 导航收敛

- 侧栏移除「整体进度」；`/progress` 兼容路由 → `<Navigate to="/planning" replace/>`
- 学习规划页面结构 = ①学习日历（顶部主视图）→ ②日期详情抽屉（**get_day_detail**：当日任务/Session 时长·摘要·附件数/验证/总时长/AI 复盘这一天）→ ③目标/阶段/计划（Stage/Plan CRUD）→ ④精简客观进度（4 Donut）→ ⑤最近学习（可点开知识）
- 不再两页重复展示月度学习/完成比例/日历分布

## 3. DEV-0302/0303 计划任务与 Today

- Planning = 计划源头（Calendar 内新建/编辑/重复规则管理 daily·weekly/启停），materialize 生成 Today；title-only 创建已在前批达成
- Today：任务行 = 勾选/标题/知识/时间/开始学习/⋯（编辑·删除·改期·归档）；连续创建保留；删除 = 无历史物理删/有历史"移除并保留历史"人话（无红条阻塞）

## 4. DEV-0304 先学，再归档

- 删除"开始学习先选知识"阻塞弹窗；无知识任务点开始 → **start_quick_session** 直接进编辑页
- Today Header 新增「⚡ 快速学习」统一入口
- 学习编辑页：进入即写（文字/图片/视频/画图/粘贴/拖拽，无知识也可用——附件挂 Session）
- **结束归档确认层**：仅保留学习记录 / 关联已有知识 / 新建知识（可选父级）/ 关联任务（自动带入）/ 可选"写入知识正文"——不强制归档
- 从知识进入：默认预选该知识；从任务进入：任务自动带入

## 5. DEV-0305 知识树主导

- 树 = 主入口（图谱降为次级切换按钮）
- **拖拽**：拖节点到目标 = 成为子节点（改父子关系，拒自身后代）；行 hover ↑/↓ = 同级手动排序（reorder_learning_items 写 sort_order）；列表查询 ORDER BY sort_order, id
- 叶子节点点击即开内容区（标题+正文+学习记录+附件+继续学习/AI/记录验证保持）

## 6. DEV-0306 Review 聚焦今天

时间线（Task+Session 按时间）→ 真正学到的内容（卡片+六操作）→ 简洁总结 → AI 复盘一行；重复统计已删（前批）。

## 7. DEV-0307/0308/0309

- 联动：规划→日历→Today→学习（快速/关联）→复盘/知识；点击日历日期看真实记录（get_day_detail）
- AI：保持全局右栏；日期抽屉"AI 复盘这一天"；无自动写入（Write Tools 0）
- 设置数据管理保持（7 清理+备份+归档任务），入口清晰

## 8. Tests / Gate

198/198（既有 190 + batch031 8；v012 后 Option 适配不改语义）；TS 0 error；cargo check 0；
`latest v012` 实机升级成功；Desktop 0 污染；真实 DB 无测试数据；Runtime 危险扫描 CLEAN；零新增依赖。
