# AI 操作记录 0012 - DEV-0012：Evaluation Workflow V2

- 时间：2026-08-15（Asia/Shanghai）
- 操作编号：0012
- 关联任务：DEV-0012（详见 `.higher/TASK.md`）
- 前置：DEV-0011（V2 五大界面）+ DEV-0011-A（验收：条件通过，负责人已调整策略为功能优先）

---

## 1. 任务目标

把 Evaluation 从数据库实体能力变成正常学习流程中的真实功能：
学习 → 结束 Session → 直接验证 → Learning Review 自动读取 → Progress 自动汇总。
功能优先策略首个任务；不新增一级导航；Schema 保持 v006（未新增 Migration / Entity / AI）。

## 2. 工作区确认

当前工作目录：`C:\Users\37653\Desktop\Higher`（所有命令前 Set-Location）。
启动后桌面复查：零新增项目临时文件。

## 3. EvaluationModal（统一组件）

`src/components/EvaluationModal.tsx` 新建：
- 验证方式分段单选：练习 / 测试 / 回忆 / 应用（沿用四类型，不扩）
- 结果分段单选：通过 / 部分掌握 / 未通过 / 未评级（沿用四结果，不扩 mastery_probability 等）
- 可选标题（默认缺省为"〈类型〉验证"）；可选总题数 / 正确数（正整数、correct≤total 前端校验，复用后端既有校验）
- 自动带入 goalId + learningItemId（Today / Knowledge 均不要求用户重选 Goal）
- 错误处理：无关联知识→"当前没有关联知识"；跨 Goal/FK 后端错误→中文可理解提示
- 今日任务与知识体系复用同一组件，无重复逻辑

## 4. Today → Evaluation

- 结束学习后"本次学习已结束"卡片：任务名 + 本次/今日累计时长 + 三动作：
  [记录一次验证]（主）→ EvaluationModal（Task→LearningItem→Goal 自动上下文）
  [继续整理知识] → /knowledge?goal=&item= 定位
  [完成任务] → 走完成前检查
- 保存成功：留在 Today，轻提示"已记录验证：回忆 · 部分掌握"（4s 消失）；不跳 /evaluations
- 结束 Session ≠ 完成 Task 原则不变（完成是独立动作）
- 完成前轻提示（不强制）：该 Task 有 Session 且其知识无任何 Evaluation →
  "这项学习还没有验证记录"（记录验证 / 仍然完成 / 取消）；查询失败不阻塞；无 Session 直接完成

## 5. Knowledge → Evaluation

- 知识详情统计行新增小型"记录验证"入口（替换原跳 /evaluations 按钮；避免按钮墙）
- 新增"最近验证"轻量区：list_by_learning_item 最近 5 条（日期/类型/结果含题数比、结果分色）；
  空态："还没有验证记录 + [记录第一次验证]"
- 创建成功后 reloadItemEvidence 刷新节点统计与最近验证

## 6. Review 联动

零改动即生效：Review 基于既有 list_by_date_by_profile 自动读到新建验证（今天的验证区域）；
failed 自动满足既有确定性规则 → "需要关注：xx 今天一次验证未通过"；partial 如实显示。未生成任何 AI 分析。

## 7. Progress 联动

零改动即生效：stats_by_profile（按类型/按结果）自动统计新验证；无第二份 Evaluation 数据。
未为 Progress 创建任何副本表/副本接口。

## 8. Profile Scope

Evaluation→Goal→Profile 归属链未变；create_evaluation 后端既有跨 Goal 校验继续生效
（B 档案 Goal + A 档案 Item 拒绝，测试覆盖）；前端仅是入口，不做"只靠隐藏"。

## 9. Evidence 与 Mastery 分离

Evaluation passed 不自动改 LearningItem.mastery_status（一次通过 ≠ 永久掌握）；
测试断言 passed 后状态仍 not_started。

## 10. 测试（新增 tests/evaluation_workflow.rs，5 用例）

与既有测试不重复（evaluation_system 覆盖 CRUD/跨 Goal/校验；review_progress 覆盖按日隔离/统计/解链）：
1. V2 端到端：Session→验证→ list_by_learning_item / 按日查询 / stats / 节点统计四处读到同一条 Evidence；相邻节点读不到
2. failed 当天可查 → Review"需要关注"数据源成立
3. passed 不改 mastery_status
4. 无 item 全科验证：只进 Review/Progress，不进任何 Item 详情与统计
5. 跨 Profile 上下文（B Goal + A Item）后端拒绝

## 11. Schema

保持 v006。启动日志：`database already up to date (latest v006)`。未创建 v007。

## 12. Tauri 实机

`npm run tauri dev` → app.exe 正常启动运行。
AI 无法观察 WebView：Modal 交互 / 轻提示 / 最近验证显示需人工验收（未伪造 PASS）。

## 13. 已知问题 / 未完成项

- Review 页"记录验证"快捷入口（TASK §20 可选项）未做——优先保证 Today + Knowledge 两入口，控制范围
- /evaluations 兼容页保留（兼容/调试/完整历史管理）；正常主流程不再要求用户去该页录验证
- Goal target_date 规格缺口沿用 DEV-0011 记录（禁 v007，未动）

## 14. 最终状态

DEV-0012 完成：学习→验证→复盘→进度 Evidence 链路打通；95/95 测试；TS 0 error；
桌面启动正常（v006）；桌面零污染。停止开发，等待负责人确认是否进入 DEV-0013 Feedback Loop V1。
