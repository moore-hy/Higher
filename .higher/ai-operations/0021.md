# AI 操作记录 0021 - DEV-0021：AI Proposal / Diff / 用户确认写入（BATCH-02 6/6）

## 1. 入口

- Session 结束摘要：✨ AI 帮我整理知识
- Knowledge 详情：✨ AI 帮我整理
- 均打开统一 AiProposalReview（内部 aiAnalyze("knowledge_organize")）

## 2. Proposal JSON（prompts.rs 强约束）

- operations 仅允许 update_content / create_child（**禁止 delete/move/rename**）
- 最多 5 条；learning_item_id / parent_id 必须来自真实节点；reason 必填思路
- prompt 明确：保留用户内容价值、不捏造事实、Session Note 原文不会被修改

## 3. Proposal = 临时数据

无 ai_proposals 表；仅 React state；关闭页面即丢弃（任务书明确接受的轻量设计）。

## 4. AiProposalReview（组件）

- update：双栏 Diff（当前内容只读 pre ↔ 建议内容可编辑 textarea）+ 理由
- create：父节点 / 新节点名（可编辑）/ 内容（可编辑）
- 单项：[接受 / 编辑后接受 / 拒绝]；状态徽章（已应用/已拒绝）
- 全部接受：二次确认（将执行 修改 N 个节点 · 新增 M 个节点）[确认执行][取消]
- 前端守卫：allowedIds = 当前档案 items 集合；目标不在集合的 operation 直接过滤

## 5. Apply（AI 不执行）

用户确认后前端调用现有正式 API：
- update_content → updateLearningItemContent(id, content)
- create_child → createChildLearningItem(goal, parent, name) + updateLearningItemContent(created, content)
后端 Repository 再次强制 Profile/Goal/parent 校验（跨 Profile 拒绝——测试覆盖）。

## 6. Session Note 永久原始

AI 整理绝不写 study_sessions.note（完整闭环测试断言 note 原文不变）；
用户自己可编辑/清空（DEV-0017 已提供）。

## 7. 测试（ai_foundation.rs）

Proposal Guards（allowed 集合 / 跨 Goal parent 拒绝 / 合法路径）+
**完整闭环**：Profile→Goal→Knowledge→Task→Session→Note→Attachment→End→
Knowledge 学习记录查询→模拟 Proposal→用户确认（正式 Repository）→Knowledge 更新→
Note 未被改动。全程同一套正式数据，无 Mock 第二套。
