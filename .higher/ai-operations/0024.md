# AI 操作记录 0024 - BATCH-03 / DEV-0024：Learning Editor V2

- 时间：2026-08-15（Asia/Shanghai）；Schema 保持 v010 内（无本 DEV Migration）

## 1. Word-like 轻量编辑器（[LearningEditor.tsx](file:///C:/Users/37653/Desktop/Higher/src/components/LearningEditor.tsx)）

- LearningDocument：轻量 Block Document（任务书 §16 方案 B）。块 = 文字（textarea 自动增高）｜图片｜视频｜画图
- 持久化：study_sessions.note TEXT 存 `{"v":2,"blocks":[{t:"text",c}|{t:"image"|"video"|"drawing",a,n}]}`；
  **未建 session_note_blocks 表**；旧纯文本完全兼容（读取=单文本块）
- 用户界面零 token/JSON/内部 ID；删除媒体块=仅移除 Note 引用（物理删除走附件区）

## 2. 内联媒体（正文位置插入，非附件区堆底）

- Ctrl+V 截图：clipboardData image → blobToBase64 → add_attachment_from_base64 → 插入当前 focus 块后
- 鼠标拖入 png/jpg/jpeg/webp/gif（+mp4/webm/mov/mkv）：dataTransfer.files → base64 → 附件 → 插入；
  拖拽视觉反馈（虚线框+遮罩"松开以插入"）
- 上传图片/视频：dialog 选源文件 → Rust 复制进 Sandbox → 插入；画图：现有 Canvas PNG → 插入
- 图片显示：左对齐 / max-width:100% / height:auto 保持比例 / 段落间距；小图不拉伸；点击看原图
- 视频：HTML5 `<video>` 内嵌（不自动播放；禁系统播放器）

## 3. 后端

- 新命令 `add_attachment_from_base64`（WebView 读取粘贴/拖入数据→复制进 Higher Sandbox；Sandbox/Profile/Session 校验沿用 AttachmentRepository）
- base64_decode 通用化（任意 data:*;base64 前缀剥离）
- 新模块 [note.rs](file:///C:/Users/37653/Desktop/Higher/src-tauri/src/repository/note.rs)：parse_blocks / plain_text /
  text_len / media_counts / media_ids / serialize_blocks（含 3 个内嵌测试）
- 前端 [utils.ts](file:///C:/Users/37653/Desktop/Higher/src/utils.ts) 同规则实现（parseNoteBlocks/notePlainText/noteTextLen/
  noteMediaCounts/noteSummary/fileToBase64/blobToBase64）

## 4. Autosave / End Flush

- 900ms debounce（任何变化：文字/插入/删除块）；状态 未保存…/正在保存…/已保存✓/保存失败·重试
- 结束学习前强制 flush：**保存失败不结束**（提示"笔记尚未保存，暂不结束学习"+重试）；beforeunload 兜底
- 附件区保留为"本次附件汇总"（媒体主要呈现于正文）

## 5. AI Context Compatibility（§29）

- session_detail_block / item_recent_notes_block / read_session 工具：结构化 note → 用户可读纯文本
  （媒体→[图片]/[视频]/[画图] 占位）+ 附件 metadata；**绝不把 JSON/token 发给模型**（测试断言）

## 6. Session → Knowledge 学习记录

- 结束摘要：时长 / 文本字数（不含媒体）/ 图片数 / 附件数；结束后可回看全文（只读编辑器）
- Knowledge 学习记录展开用 NoteView（文字+内联媒体只读渲染，[NoteView.tsx](file:///C:/Users/37653/Desktop/Higher/src/components/NoteView.tsx)）

## 7. Tests（batch03.rs）

- v010 schema（本批首测）+ 旧 Task 新列 NULL
- note v2 保存/读取/解析/纯文本/字数/媒体统计/媒体 id
- 旧纯文本兼容 + AI context 纯文本提取（含"无 JSON"断言）+ v2 note AI 提取
- 附件归属（session+item）与跨档案 session 拒绝（Session/Profile Scope）
