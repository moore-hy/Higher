# AI 操作记录 0028 - BATCH-03 / DEV-0028：Knowledge Workspace + Editable Knowledge Graph V2

- 时间：2026-08-15；无 Migration（禁 graph_nodes/graph_edges；关系仍 learning_items.id + parent_id）

## 1. Workspace 内容优先（§85 新顺序）

Header（面包屑/名称/状态/开始学习/✨AI 检查/✨AI 整理/记录验证）→
**我的知识**（最大主编辑区：新「我的知识」标签 + 正文 textarea + 自动保存状态；引导文字仅空态 placeholder）→
学习记录 → 知识附件 → **学习证据**（`<details>` 默认折叠：最近验证 + 需要关注）。
统计压缩进 Header 一行（累计学习/次数/最近/验证次数）。

## 2. Editable Knowledge Graph（[KnowledgeGraph.tsx](file:///C:/Users/37653/Desktop/Higher/src/components/KnowledgeGraph.tsx)）

- 从只读查看器升级为可操作 View：**单击选中**（accent 描边+顶部操作栏：名称/状态/打开/新增子知识/重命名/移动到…/删除）；
  **双击 = 打开**；每节点 ⋯ 菜单（同五项）
- **新增子知识**：正式 createChildLearningItem（Goal 继承父节点）→ Graph 与左侧树立即一致（同一 items state refresh）
- **重命名**：正式 updateLearningItem（写 learning_items.name；图不维护第二份 label）
- **移动到…**：Knowledge 选择器（列表排除自身+后代+跨 Goal；含"作为顶级知识"）→ 正式 move_learning_item
- **删除**：确认 → 正式 safe delete（存在 child/Task/Session/Evaluation/Attachment 按现有 Guard 拒绝，人话提示）
- Graph→Workspace：打开/双击切换视图并定位同一 learning_item；Workspace→Graph：切换后当前节点 accent 高亮
- SVG 简单树布局保留（折叠/滚动）；无拖拽移动/动画/力导向/3D（§100 非硬性要求，未拖延批次）
- 前端移动目标过滤 + 后端双重校验（拒绝自己/后代/跨 Goal/跨 Profile/非法 parent）

## 3. 后端

- LearningItemRepository.move_item（§98 全部拒绝条件 + 100 层深防御）；命令 move_learning_item
- Knowledge 页 wire：onCreateChild/onRename/onMove/onDelete 全走正式 API + refresh

## 4. Tests（batch03.rs）

create child / rename / move 根↔子 / move self 拒 / move 后代拒 / move 跨 Goal 拒 / move 不存在拒 /
有子节点 delete guard / 同源（list_by_goal 含新节点）/ 跨 Profile move 双向拒绝。
