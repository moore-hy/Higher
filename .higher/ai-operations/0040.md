# AI 操作记录 0040 - BATCH-04 / DEV-0040：Profile First 数据模型（v013）

- 时间：2026-08-15；Migration **v013**（本轮唯一授权）

## 1. v013（[v013_profile_first.rs](file:///C:/Users/37653/Desktop/Higher/src-tauri/src/migrations/v013_profile_first.rs)）

6 表全部直挂 `profile_id NOT NULL`（不再经 goals JOIN 推导）：
- **tasks** 重建：+profile_id；goal_id→可空
- **study_sessions** 重建：+profile_id；goal_id→可空；**+title**（Quick="快速学习"/Task=task.title/Knowledge=item.name）；+time_corrected
- **learning_items** 重建：+profile_id；goal_id→可空
- **evaluations** 重建：+profile_id；goal_id→可空
- **recurring_task_rules** 重建：+profile_id；goal_id→可空；learning_item_id→可空
- **learning_attachments** 重建：+profile_id（COALESCE(item 链, session 链) 回填）

Backfill：INSERT SELECT 全保 id；无法推导 profile → 自检计数>0 → Err 回滚（HARD STOP 条件，不塞 active profile）。
迁移末尾 `PRAGMA foreign_key_check` 必须 0。

**关键修复**（迁移框架）：`run_migrations` 在每个迁移事务外 `PRAGMA foreign_keys=OFF/ON`
——SQLite 对 DROP TABLE 做隐式 DELETE 会触发未迁移旧表的 FK 级联（v013 重建顺序下曾静默丢 tasks 行）；
PRAGMA 在事务内无效，必须在事务外切换。

## 2. Repository Profile-first 重写

- task.rs：`create_for_profile(profile_id, goal_id: Option, ...)` / `create_quick_for_profile` / `create_from_rule(+profile)`；全部 *_by_profile 直查 profile_id；update 校验 item↔profile
- study_session.rs：`start_quick(profile_id)`（删除 goals 回退链——顺带消除引用不存在列 `study_profiles.is_active` 的死查询）/ `start_for_task` / `start_for_item`；+`update_title`/`correct_time`(重算 duration+标记)/`unlink_item`/`delete`/`list_session_attachment_paths`
- learning_item.rs：`create_for_profile`；move 校验跨档案；list_by_profile/status_counts 直查
- evaluation.rs / recurring_rule.rs / attachment.rs：create 前置 profile_id；goal/item 全可空；校验改跨档案
- insight.rs / mod.rs(DayDetail，ss.title 优先) / study_profile.rs(get_calendar) / cleanup.rs(FullReset 按表逐删 profile_id 直删，不再 DELETE FROM goals 级联；修复 v012 后 NULL-item session 清理丢失)

## 3. AI Profile-first（§19）

context.rs / tools.rs 全部改 profile 直查；无知识 Session 现在进入 AI 上下文（LEFT JOIN）；
`get_current_goal` 无 Goal 返回 `{"goal":null,"note":"当前未设置长期目标"}` 不报错。

## 4. lib.rs commands

- `create_task(profile_id, goal_id: Option, ..., planned_time)` / `create_recurring_rule(profile_id, ...)` / `create_evaluation(profile_id, goal_id: Option, ...)` / `start_quick_session(profile_id)`（移除目标回退）/ `create_root/child_learning_item(profile_id, goal_id: Option, ...)`
- 新增：`start_task_session` / `update_session_title` / `correct_session_time` / `unlink_session_item` / `delete_session`（事务删+Session 专属附件文件清理，不动 Knowledge 正文）
- 附件目录改 `attachments/<profile>/item/<id>/` 或 `attachments/<profile>/session/`（旧 relative_path 保留于 DB，可继续读取）

## 5. 测试（tests/batch04.rs 新增 6 用例，全部通过）

Goal-free 全链（含 AI 上下文无 Goal Error）/ Quick Study 全链 / Archive Later /
v012→v013 迁移（7 类实体 ID 保留 + profile 回填正确 + fk_check=0 + title 回填验证）/
五实体双档案隔离 + 跨档案拒绝 ×3 / 历史编辑（title/time/unlink/delete）。

既有 19 套件适配新签名后全绿；总计 **180+6=... 见最终报告统计**。
SAC：本轮 ai_foundation/ai_panel/insight_review 3 套件曾被拦（单次确认即标 ENV_BLOCKED，未重试；此前编译版本均通过）。

## 6. 发现并修复的既有缺陷

1. v013 初版迁移在 FK=ON 下重建表会级联丢数据 → run_migrations 事务外关 FK（上述）
2. v013 初版漏 time_corrected 列 → 已加入表定义
3. `list_by_profile` 一处 JOIN goals 残留（批量替换失效）→ 已修
