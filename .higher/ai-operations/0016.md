# AI 操作记录 0016 - DEV-0016：Higher 2.0 Definition + Settings Center（BATCH-02 1/6）

- 时间：2026-08-15（Asia/Shanghai）
- 关联任务：BATCH-02（DEV-0016～0021 连续批次，一次性授权）

## 1. Higher 2.0 产品定义（正式写入 PROJECT.md）

产品定义 + 四条最高原则（Knowledge First / AI Advisory / Low Friction / Open System）
+ 正式主链（Goal→Plan→Today/自由→Session→Session Note→Knowledge→AI Context→AI Advisor→
AI Proposal→用户接受/修改/拒绝→下一轮学习）+ Evaluation→Feedback→Adjustment 重新定位为
辅助 Evidence（详见 PROJECT.md 同步记录）。

## 2. Settings 入口

- Sidebar 底部「⚙ 设置」（/settings），五学习入口不变，无 AI 一级导航
- Settings.tsx 双 Tab：学习档案 / AI 设置

## 3. 学习档案区

复用现有 Profile API（update/create + canSwitchProfile/exit/enter），无第二套逻辑。

## 4. AI 设置（DeepSeek）

- 字段：Provider(固定 deepseek) / Base URL(默认 https://api.deepseek.com) /
  API Key（**明文输入/显示/保存**，无 password input/脱敏/加密——个人本地软件）
- Model：下拉 deepseek-v4-flash / deepseek-v4-pro + 自定义输入
- Thinking Mode 开关（默认关；开启时模型名按 -thinking 后缀发送，出错给人话提示）
- 测试连接：Rust 真实调用（max_tokens=8），人话错误映射
（401 Key 无效 / 402 额度 / 404 模型或地址 / 429 频率 / 网络 / JSON 异常）

## 5. 存储

复用 settings KV（v001 表，无新 Migration）。keys：ai.provider / ai.base_url /
ai.api_key / ai.model / ai.thinking_enabled（repository/setting.rs）。
Rust 提供 get_ai_settings / save_ai_settings / test_ai_connection。
**API Key 不写 console / log / 错误堆栈 / ai-operations**。

## 6. 新依赖

Rust：reqwest 0.12（json + rustls-tls，default-features off，无大型 SDK）；
tauri-plugin-dialog 2（DEV-0018 文件选择）。npm：@tauri-apps/plugin-dialog。
capabilities/default.json 增加 dialog:default。

## 7. 结果

tsc 0 / cargo 通过；文档见 PROJECT.md Higher 2.0 章节。
