# AI 操作记录 0026 - BATCH-03 / DEV-0026：Planning Calendar + Recurring Tasks

- 时间：2026-08-15；**Migration v010**（本批唯一，见 0024 内 schema 测试）

## 1. Migration v010（[v010_recurring_tasks.rs](file:///C:/Users/37653/Desktop/Higher/src-tauri/src/migrations/v010_recurring_tasks.rs)）

- 新表 recurring_task_rules（goal_id FK CASCADE / learning_item_id FK CASCADE / title / repeat_type /
  weekdays_json / time_of_day / start_date / end_date / enabled / 时间戳；goal 索引）
- tasks + planned_time TEXT NULL、recurring_rule_id INTEGER NULL（**无 FK**——不重建 tasks 表，
  按 §9 用 nullable integer + Repository Guard + idx_tasks_rule_date 索引保证关系）
- 安全：不删任何数据；旧 Task 两列 NULL；实机 `latest v010` 升级成功且旧数据保留
- repeat_type 仅 daily/weekly（校验层禁 monthly/cron/RRULE）

## 2. Planning 双视图

顶部 [学习路线] [学习日历] 切换；学习路线原样保留；日历直接读正式 Task/RecurringRule（非第二套数据）。

## 3. Calendar（[PlanningCalendar.tsx](file:///C:/Users/37653/Desktop/Higher/src/components/PlanningCalendar.tsx)）

- 月历：← 2026年8月 → 今天；周一起始网格；cell = 日期 + 完成/总数 + 最多 3 条（✓/○ 标题）+ "+N"；今天高亮
- 点击日期 → Day Panel（Modal）：友好日期标题；任务行 checkbox 完成/取消、编辑（TaskModal）、删除、
  开始学习；「+ 新建任务」planned_date 自动=选中日期
- 与 Today 同一套正式 Task API——今天两处完全一致（禁 CalendarTask 实体）

## 4. Recurring Rules

- 入口：日历页「重复任务（启用数）」折叠区；规则行 = 名称/频率(每周一·三·五)/时间/起止/知识 + 启用/停用/编辑/删除
- Rule Modal：名称*、每天|每周（星期多选 chips）、关联知识*（搜索）、开始日期*、结束日期（可选）、时间（可选）
- 删除规则：历史已生成 Task 保留（测试）；修改只影响未来 materialization
- 时间语义：Higher 内任务出现时间——无闹钟/Toast/后台服务/开机启动

## 5. Materialization（幂等）

- 后端 `materialize_recurring_tasks(profile_id, date)`：对启用规则按 daily/weekly(weekday) 匹配当日，
  **(rule_id, planned_date) 已存在即跳过**——刷新/重开应用/重复调用均不重复（测试：三次调用仅首次建 1 条）
- 触发：Today 与 Calendar 加载时 + 运行中每 30s Timer（新任务>0 才刷新）；无时间规则当日首次加载生成
- App 未运行不后台 materialize；下次启动补生成
- 星期算法：Sakamoto（无 chrono）；测试验证 2026-08-15=周六 / 17=周一 / 16=周日

## 6. Profile Scope

规则无 profile_id——经 goal_id→goals.profile_id 链追踪；list_by_profile / materialize 均按档案过滤（测试：B 档案 0 规则 0 生成）。

## 7. Tests（batch03.rs）

v010+旧数据 / weekday / daily 幂等×3+未来日期 / weekly 星期匹配+空选拒绝+非法值+非法类型 / start·end·disabled·edit·倒置拒绝 /
删规则留历史+档案隔离 / AI list_tasks 读重复任务（planned_time+from_recurring）。
