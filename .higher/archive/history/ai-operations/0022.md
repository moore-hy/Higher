# AI 操作记录 0022 - BATCH-02.1 / DEV-0022：AI Agent Panel + Runtime Sandbox + 产品整合验收

- 时间：2026-08-15（Asia/Shanghai）
- 关联任务：BATCH-02.1（单任务 DEV-0022；Schema 保持 v009，无 v010）

## 1. AI Agent Panel

- `src/components/ai/AiPanelContext.tsx` + `AiPanel.tsx`（React Context + useState，无大型状态框架）
- 全局右栏（Layout 三栏：导航 | 工作区 | Higher AI）；360px 固定宽（min 320 / max 440，无拖拽）
- 展开/收起：settings KV `ui.ai_panel_open`（新增 get/set_ui_setting 命令，**仅允许 ui. 前缀**——
  防止通过该通道读取 ai.api_key）；缺省 false；收起时右下 ✨AI 悬浮按钮
- Header：Higher AI / ＋新建对话 / ✕收起 / 模型下拉（v4-flash·v4-pro·自定义，与 Settings 同源
  save_ai_settings，无第二套 model state）/「AI 设置 ›」跳 /settings
- Context Header：当前档案 / 当前页面 / 当前知识（面包屑）/ 当前学习——用户永远知道 AI 在看什么
- 页面上下文上报：Today/Planning/Review/Progress（page+goal）、Knowledge（+item+路径）、
  Learning Workspace（+item+session）；Settings 不提供业务上下文

## 2. 对话

- 多轮（仅 React Session 内存；无 ai_conversations/ai_messages 表——明确接受的轻量设计）
- 预算：最近 6 turn / ~8000 字符（trimHistory 丢最旧）；业务上下文每次由后端 AiContextBuilder 重建
- @ scope Chips：当前页面/当前知识/本次学习/当前规划/整个档案（仅当前场景存在时显示，点击 Chip 即可）
- 输入栏：[@chips] [输入] [发送] + 模型行；busy 禁重复发送；错误人话（humanizeError）+ 重试入口

## 3. assistant_chat（新 scope，无新 Entity/Migration）

- 自然语言（不强制 JSON）；允许只读工具，最多 6 轮（与 profile_analysis 相同）
- 提示词：只依据真实数据/不虚构/「可能·建议」措辞/无百分比打分/用户要改笔记知识只给建议不写入
- **修复发现的真实漏洞**：build_context 原先未校验可选 session/item 归属——
  已加统一 validate_optional_ids（所有 action，含 Panel 自由对话附带的 ID）→ 跨 Profile 拒绝（测试覆盖）

## 4. Tool Trace（真实，禁伪造）

- 后端 run_with_tools 重构：返回真实发生的调用列表（tool/中文label/成功|失败）；
  TOOL_ALLOWLIST 前置校验——模型给出非白名单名称直接拒绝（错误返回给模型，不进 dispatch）
- Panel 渲染：可折叠"本次使用的上下文"——已提供上下文（chips，注明"由 Higher 直接提供，非工具调用"）
  + 真实 Tool Trace 列表（✓/✕ + 中文名 + 工具名）；未发生的调用绝不显示
- 无流式（无 token streaming/SSE/WebSocket）：请求中"正在分析…"，结束展示 上下文+Trace+答案+tokens

## 5. 现有 AI 入口统一

- 五页面按钮全部改为触发 Panel runAction（自动展开+请求+结果显示在 Panel）：
  Today today_suggestion / Learning session_analysis·knowledge_organize /
  Knowledge knowledge_analysis·knowledge_organize / Planning planning_analysis / Progress profile_analysis
- 删除页面级独立 AI 结果 UI（Today 建议卡/Knowledge 检查区/Planning 检查卡/Progress 分析卡/
  AiSessionReport 组件及其渲染）——结构化 JSON 由 Panel renderStructured 统一转可读文本
- Review 无既有 AI，未新增（Panel @整个档案 可自行提问）

## 6. Proposal 集成

- knowledge_organize 结果 → Panel 消息「我建议对知识库进行 N 项修改」+ 摘要操作列表 + [查看变更]
- 点击展开**现有** AiProposalReview（新增 initialProposal 入参跳过重复请求；apply 逻辑未重写）：
  双栏 Diff 可编辑 / 逐项接受·编辑后接受·拒绝 / 全部接受二次确认 / Profile Guard（前端 allowedIds +
  后端校验）——权限不变（仅 update_content/create_child）
- 未应用 Proposal 时新建对话 → 提示「当前还有未处理的 AI 修改建议」[继续新建][取消]
- Profile 切换 → 立即清空对话/Trace/Proposal；关闭 Panel 不影响 Session/Proposal 状态
- Session Note 永不修改（AI 只能给建议文本）

## 7. Higher Runtime Sandbox

- `src-tauri/src/sandbox.rs`：
  - validate_relative：拒绝空/..（两种分隔符）/绝对路径/盘符/UNC
  - resolve_in_sandbox：静态校验 + 逐组件规范化 + starts_with(root) + 存在时 canonicalize 双重验证
  - resolve_import_source：唯一 Sandbox 外例外 = 用户在系统 Dialog 主动选择的**单个已存在文件**（拒绝目录）
- 接入：add_learning_attachment（导入源过 Guard，复制进 sandbox，不存 source_path）/
  read_attachment_image（DB relative_path 不可信，读取前过 Guard）/
  delete_attachment（即使 DB 被篡改为 ../../xxx 也无法删除外部文件）
- **移除 open_attachment_file 命令与全部 std::process 调用**；视频改为 Higher 内 HTML5 <video>
  内嵌播放（base64，仅 sandbox 文件）
- 目录结构不变：dev 全部在 src-tauri/.data（DB+attachments）；prod 唯一例外 app_data_dir

## 8. 权限与静态扫描

- AI 工具白名单 10 个只读（无 read_file/write_file/run_command/shell/powershell/cmd/spawn/
  registry/network_fetch/query_sql/execute_sql/update_knowledge/create_task/update_plan）
- AI 无 rusqlite Connection / 通用 SQL；仅业务读取函数
- capabilities/default.json：仅 core:default + dialog:default（无 shell/fs 插件与宽泛 scope）
- Cargo 无 shell/execute 类依赖；静态扫描 Runtime 代码（src-tauri/src + src）：零危险模式命中
- AI 网络仅 Rust AiClient；URL 仅来自 Settings（模型不能传 URL/改设置）

## 9. Tests（+19，总 155，0 failed）

- sandbox_guard.rs（11）：正常解析 / ../ 与 ..\ 逃逸 / C:\ D:\ 任意盘符 / UNC+Unix绝对+空 /
  sandbox 内删除 / 外部删除拒绝（真实外部文件未触碰）/ 恶意 DB 路径变体（编码/混合分隔）/
  导入源必须单文件 / Profile 附件隔离回归
- ai_panel.rs（8）：scope dispatch（页面→action；allow_tools/require_json 矩阵；未知拒绝）/
  knowledge·session 上下文携带正确 id / assistant_chat 跨 Profile session·item 拒绝 /
  history 角色过滤 / Trace 仅真实+白名单无危险工具（17 种恶意名逐一断言拒绝）/
  Proposal Guard 回归 / ui. 前缀规则

## 10. Gate 结果

- TS 0 error；cargo check 0 error/0 warning；**cargo test 155/155**
- npm run tauri dev：app.exe 正常、`latest v009`、无 panic/migration/capability error
- Desktop 零污染（db/json/log/tmp/ps1/js/截图 = 0；node_modules 无）；DB/附件仍在项目内
- 新增依赖：无（纯 React/Tauri/已有 reqwest）

## 11. 文档冲突修复（PROJECT.md）

- "未来 AI（仅设计，未实现）" → 已实现 V1；分层架构 4/5/6 层（AI Context/Proposal/Provider）
  由"未来"改为"V1 已实现"；"未来 · AI Layer（方案 TBD）" → 已实现清单 + 未来方向
- "当前 v006" → v009；"AI 接入暂不继续" 移除（已接入）；Feedback "L0 未建立" → 已建立
- 当前阶段 → Stage D0 · Product Integration & Real Use Validation；开发重点 → 产品整合+真实使用

## 12. Known Issues

- Panel 无取消按钮（reqwest+Tauri 结构下需较大改造，按任务 §66 本轮不做；busy 期间禁发防并发）
- 视频 base64 内嵌（超大视频内存占用较高；第一版接受，未做流式/文件协议）
- @ 自由对话的 @本次学习/@当前知识 scope 在 knowledge_analysis 等 scope 复用结构化 action——
  回复为结构化文本渲染（非纯自由对话）；@当前页面/@整个档案 为 assistant_chat 纯自然语言
- 对话历史不跨重启持久化（明确接受的轻量设计）

## 13. Manual Acceptance（NEED HUMAN）

见 PROJECT.md 已知问题 §10（七流程 + Sandbox 三确认）；AI 无法观察 WebView，未伪造 PASS。
