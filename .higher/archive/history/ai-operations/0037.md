# AI 操作记录 0037 - BATCH-03.1 / DEV-0037：Cross-module Workflow + Final UX Cleanup

- 时间：2026-08-15；无 Migration

## 1. 业务链验证（§109-118）

- Planning↔Calendar↔Today：同一 tasks 表（list_today_by_profile / list_by_range_by_profile 同源）✓
- Task 可以没有 Knowledge（§110 永久规则）：v011 + Quick Create ✓（测试）
- Task 开始学习无 Knowledge（§111-112）：选已有/快速新建/暂不开始（StudySession 仍需 item——
  按任务书 §112"第一版可以要求开始正式学习前再选 Knowledge"实现；**创建 Task 绝不要求**）✓
- Task 完成 → Review Timeline 出现（§114）✓；Session 结束 → Timeline 真实内容（§115）✓
- Learning→Knowledge 学习记录（§116）✓；Review→Tomorrow/Calendar 正式 Task（§117）✓
- Progress 点击日期 → Review ?date=（§118）✓；Tree↔Graph 同一 learning_item（§119）✓

## 2. AI（§120-122：未重构，仅必要兼容）

- list_tasks：LEFT JOIN items（title-only → knowledge:""/learning_item_id:null）+ **默认仅活跃**
  （archived 不进列表；`include_archived:true` 可显式查）——修复了归档任务混入 AI 结果的真实 bug
- today context：LEFT JOIN + archived 过滤（title-only 显示"未关联知识"）
- v011 适配：insight 趋势/指标、study_profile 档案日历、cleanup 全部改 tasks→goal 直连 JOIN
  （title-only 任务不再因 INNER JOIN 丢失）；AI Write Tools 保持 **0**

## 3. 全局 UI（§123-131）

- 暗色风格保留；按钮层级（Primary 蓝/Secondary 边框/Danger 红仅删除）已是现状
- 操作收纳（§125）：高频直显（+ 新建任务/开始学习），低频进 ⋯（编辑/改期/归档/删除/更多设置）——本批全部落地
- Toast（§131）：Today 页轻量全局 Toast（创建/归档/改期反馈，2.6s 自动消失；无大型框架）
- 即时性（§129）：所有 CRUD 后当前页立即 refresh；错误贴近操作（删除 Dialog 人话，§130）

## 4. Tests（batch031.rs，8 用例）

v011 升级保数据 / title-only 创建（单+五连+无日期）/ 归档生命周期全链 / Profile 隔离 /
Stage delete guard / AI 读 title-only+归档过滤 / **完整集成链**（Profile→title-only Task→完成/取消→
建 Knowledge→关联→学习→Note→结束→Review 数据→Knowledge History→Progress→Archive→历史仍在）/
兼容签名回归。

## 5. Gate

TS 0 error；cargo check 0 error/0 warning；**198/198 tests**（190+8）；
`npm run tauri dev`：`latest v011` 实机升级成功、旧 Profile/Goal/Knowledge/Session 保留
（DB 未清空、无测试污染）、无 panic/capability error；Desktop 零污染；Runtime 危险扫描 CLEAN；零新增依赖。
