# AI 操作记录 0030 - BATCH-03 / DEV-0030：Profile Data Control + Cross-module Integration

- 时间：2026-08-15；无 Migration

## 1. Settings 数据管理（第三 Tab）

明确显示当前档案名；所有操作只影响当前 StudyProfile（页面明示）。

## 2. 七种清理（[cleanup.rs](file:///C:/Users/37653/Desktop/Higher/src-tauri/src/repository/cleanup.rs)）

清除今天 / 只保留今天 / 清除本月 / 只保留本月 / 清除今年 / 只保留今年 / **清空当前档案全部数据**。

- "活动数据" = Task / StudySession / Evaluation / Feedback / Adjustment / Session Attachment（§126）
- 时间清理**永久保留**：Profile / Goal / Stage / Plan / Knowledge / Knowledge content / 知识独立附件 /
  RecurringRule（长期学习系统结构）
- Full Reset：保留档案外壳（名称等），DELETE goals（FK 级联清空 knowledge/stages/plans/tasks/sessions/
  evaluations/feedbacks/adjustments/attachments/recurring rules）→ 空白档案

## 3. Preview → 确认 → 执行

- preview_profile_cleanup（只读）：将删除 Tasks/Sessions/Evaluations/Feedbacks/Adjustments/Attachments
  真实计数（Full Reset 额外 Goals/Knowledge/Stages/Plans/Rules）；预览后数据仍在（测试）
- 普通：真实预览 + 一次确认；**Full Reset：必须输入「清空」**才允许执行

## 4. 备份（destructive 前强制）

- 先备份 → 失败则**禁止删除**（"备份失败，本次清理已取消"）
- 命名 higher-YYYYMMDD-HHmmss.db（UTC+8，无 chrono；civil 算术）；目录：dev=`.higher/backups/`、
  prod=app_data_dir\backups（只操作 Higher 自己的 backups 目录）
- Retention：最多 10 份，超出删最旧（仅匹配 higher-*.db）
- db.rs 增 DbState::database_path()（与 lib.rs dev/prod 约定一致）

## 5. 事务与附件安全（§143-145）

- execute_collecting：单个 SQLite transaction，任何一步失败 rollback
- 顺序：事务内先收集将删附件 relative_path → 分步 DELETE（adjustments→feedbacks→evaluations→
  session 附件→sessions→tasks；FullReset=goals 级联）→ commit → **DB 成功后**删 Sandbox 文件
  （Path Guard 阻止越界；文件失败不回滚 DB 仅跳过；绝不删外部文件）

## 6. 跨模块整合（§146-157）

- Planning→Today：Calendar 今天 Task = Today 同一 list_today_by_profile（测试/同 API）
- Today↔Planning：update_task 改 planned_date 双向立即反映（同一 tasks 表）
- Today→Learning：开始学习→/learn/:id（已有）；Knowledge→Learning：开始学习关联当前知识（已有）
- Learning→Knowledge：Session Note（v2）→ 学习记录摘要/完整查看（DEV-0024/0027）
- Learning→Review：结束 Session→Review 真实 Note 摘要（DEV-0027）
- Review→Knowledge/Planning：打开知识 / 加入明天 / 安排日期（正式 Task→Calendar）
- Progress→Review：30 天表点击→?date=（DEV-0029）
- Recurring→Today/Calendar：materialize→正式 Task→同一列表（AI list_tasks 同读，含 planned_time/from_recurring）
- AI 兼容：Proposal Apply 未受 UI 重排影响（update_content/create_child API 未变；allowedIds 逻辑不变）
- Profile 切换：全部新 UI 经 activeProfile/refreshKey 刷新（沿用各页既有模式）；Active Session 禁切换 Guard 保持
- 空状态：Today 无任务空态含 [+新建任务][从学习规划选择]；Calendar/其他页均有提示
- 乐观更新：Today/Calendar checkbox 即时更新+失败回滚；错误人话（humanize_repo_err）

## 7. 安全保持

AI Cleanup Tool：**未新增**（AI Write Tools 保持 0）；Cleanup 仅 Settings UI 显式操作；
Sandbox/PathGuard 全保持；runtime 危险模式扫描 CLEAN。

## 8. Tests（batch03.rs）

preview 计数+只读 / clear_today（今天清、昨天留、结构留、附件文件收集）/ keep_today /
clear_month（跨月保留）/ keep_year（去年删、今年留）/ full_reset（外壳留、goals/knowledge/rules/tasks 全清）/
Profile 隔离（清 A 不影响 B）。
