# AI 操作记录 0031 - BATCH-03.1 / DEV-0031：Today Quick Task + Task Lifecycle Repair

- 时间：2026-08-15；**Migration v011**（任务书 §六授权的唯一场景，见下）

## 1. 事实核对（先行）

实测 v002 的 `tasks.learning_item_id` 为 **NOT NULL**（SQLite 无法安全 DROP NOT NULL），
title-only Task 无法在现有 Schema 落库 → 触发 §六授权的 v011。

## 2. Migration v011（[v011_task_lifecycle.rs](file:///C:/Users/37653/Desktop/Higher/src-tauri/src/migrations/v011_task_lifecycle.rs)）

- 仅解决两件事（§六白名单）：`learning_item_id → 可空`（title-only 永久规则 §110）+ `archived_at TEXT NULL`
- 实现零破坏：`tasks_new`（含 goal_id NOT NULL 直接归属 + 全列）→ `INSERT SELECT` 复制全部行（id 不变，
  study_sessions.task_id 引用有效）→ DROP 旧表 → RENAME → 重建索引；Migration 事务内
- 实机验证：`latest v011` 升级成功，旧 Profile/Goal/Knowledge/Session 全部保留（DB 156KB 未清空）

## 3. Task Repository 重做（[task.rs](file:///C:/Users/37653/Desktop/Higher/src-tauri/src/repository/task.rs)）

- **Quick Create**：`create_quick(goal_id, title, date, item?)` —— 唯一必填 = 标题（§八）
- goal_id NOT NULL：Profile 归属不再依赖 knowledge 链（title-only 也能按档案隔离）
- **删除语义（§18-20）**：无 Session → 物理删除；有历史 → `delete` 返回 `Ok(false)` → 前端改走 `archive`
- **archive/unarchive/list_archived**：归档不出现在 Today/Calendar/普通列表/Recurring 活跃统计；
  Session/Review/Progress 历史完整保留（§21-22）
- 兼容签名保留（§132 不删旧测试）：`create(item,title,date)` / `create_with_plan_legacy` /
  `list_today()` / `list_all()` / `list_all_by_profile(pid)`（内部解析 goal）

## 4. TaskModal 重做（Quick Add；[TaskModal.tsx](file:///C:/Users/37653/Desktop/Higher/src/components/TaskModal.tsx)）

- 第一屏只有：**任务名称\*（自动聚焦）+ 日期（默认今天）**；其余全部收进「更多设置」（§9-10）：
  时间 / 关联知识（**可选**：搜索 + 快速新建 + 清除关联）/ 重复（仅新建：每天|每周+星期多选）
- Enter=创建；Esc=关闭（§14）；**「创建并继续」**连续添加（§13）
- 知识关联是增强不是前提（§12）——不选也能创建

## 5. Today 页精简（V3）

- Header（§24）：标题 + 一行摘要 `N / M 已完成 · 学习 Xh Ym` + **[+ 新建任务]** 常驻
- 任务列表主体（§15）：`☐ 标题 [时间·知识小标签·重复] | [开始学习] [⋯]`（不再一排大按钮）
- **⋯ 菜单（§16）**：编辑 / 改到明天 / 选择日期… / 归档 / 删除；checkbox 即完成/取消（乐观更新）
- 删除确认（§19-20/§130）：贴近操作的人话——"会改为『从任务列表移除并保留学习历史』，学习时间与笔记不会丢失"
- **开始学习无知识（§111）**：弹"这次学习要记录到哪里？"→ 选已有知识 / 快速新建（创建并开始）
- AI 今日建议压缩一行（§25）；轻量 Toast（创建/归档/改期反馈）
- 当前学习卡保留（含跨天提示）

## 6. Tests（batch031.rs）

title-only 创建（数学单建 + 数学/英语/408/背单词/看网课五连 + 无日期）/ 归档生命周期
（无历史删/有历史→false→archive→活跃列表消失→历史 Session 保留→归档列表可见→恢复）/ Profile 隔离 /
v011 升级保数据（nullable+archived_at）/ AI 读 title-only+归档过滤 / 兼容签名回归。
