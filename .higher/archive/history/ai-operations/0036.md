# AI 操作记录 0036 - BATCH-03.1 / DEV-0036：Profile Data Control Completion

- 时间：2026-08-15；无 Migration

## 1. 入口与范围（§100-104 保持）

Settings → 数据管理（第三 Tab）；顶部明示当前档案名 + "以下操作不会影响其他档案"。
七种清理（清除/只保留 今天/本月/今年 + Full Reset）语义与 BATCH-03 相同：
活动数据 = Task/Session/Evaluation/Feedback/Adjustment/Session 附件；长期结构永久保留。

## 2. Preview / Backup / Full Reset（§105-107 保持）

真实计数预览（只读）→ 先备份失败禁删 → 单事务删除；Full Reset 巨大警告 + 输入「清空」。

## 3. 已归档任务管理（§23 新增）

数据管理内折叠区「已归档任务」：列表（标题/归档日期/原计划）+ **恢复**（unarchive → 回活跃列表）。
不新增一级页面；永久删除走"该任务无历史依赖"的常规删除路径（有 Session 历史的任务不可物理删）。

## 4. 最近备份（§108 新增）

折叠区「最近备份」：`higher-YYYYMMDD-HHmmss.db · X.XX MB`（title 显示完整路径；最多 10 份）。
命令 `list_backups`（BackupInfo{name,size_bytes,path}；仅读 Higher 自己 backups 目录）。
**本轮不开发恢复 API**（按 §108：明确告知"备份已保存"，恢复需手动替换文件）。

## 5. Tests

batch03 已覆盖全部清理场景（preview/clear·keep today·month·year/full reset/Profile 隔离）；
batch031 覆盖归档生命周期（含恢复）。
