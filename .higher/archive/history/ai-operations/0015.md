# AI 操作记录 0015 - DEV-0015：Insight & Long-term Review V1（BATCH-01 第 3/3）

- 时间：2026-08-15（Asia/Shanghai）
- 操作编号：0015
- 关联任务：BATCH-01（Gate 2 通过后自动进入）
- 结果：Gate 3 通过；BATCH-01 完成（详见文末）

---

## 1. 任务目标

把已有真实数据整理成用户可理解的长期反馈：复盘时间窗口（今天/本周/当前阶段）、
周期聚合、30 天趋势、下一步。**不创建 Migration（Schema 保持 v008，禁 v009）**。

## 2. 复盘时间窗口（Review.tsx）

- 顶部窗口切换：今天（默认）/ 本周 / 当前阶段
- 代码内部统一 start_date/end_date（range），不写三套独立页面
- 本周 = 本周一 → 今天（本地时区）；当前阶段 = stage.start_date → min(today, stage.end_date)
- 阶段无 start_date → 如实显示"当前阶段缺少时间范围"（不伪造日期）
- 所有区块标题随窗口动态（本周推进 / 阶段内解决 等）

## 3. 周期聚合（Repository 层新增，避免 React N+1）

- list_by_range_by_profile：Session（started_at）/ Evaluation（occurred_at）/ Task（planned_date）
- Feedback：list_created_by_range（created_at）/ list_resolved_by_range（resolved_at）
- Adjustment：list_created_by_range（created_at）
- 全部 Profile Scope（JOIN goals）

## 4. 周期内推进 / 问题（UI）

- 概览统计随窗口（完成任务/学习次数/时长/知识数/验证数 + Feedback 增/解 + Adjustment）
- 需要关注：窗口内 open Feedback 按知识聚合展示（FeedbackCard，可安排重新学习/解决/忽略）
- 周期内解决（独立区：标题 + created→resolved 日期区间，看到自己解决了什么）
- 周期内调整（问题 → 调整 → 执行链 + 状态徽章含日期）

## 5. Progress 增强

- 当前需要关注：待处理 Feedback / 本周新增 / 本周解决 / 待执行 Adjustment（真实计数）
- 最近 30 天趋势：InsightRepository::learning_trend_by_profile 单 SQL 递归 CTE 聚合
  （completed_tasks/session_count/study_seconds/evaluation passed·partial·failed/
  feedback_created·feedback_resolved），纯 CSS 柱状 + 悬停明细 + 周期汇总（无图表库）
- 下一步：next_actions_by_profile（planned Adjustment → 未完成 Task，含来源问题标题，
  可解释 Evidence→Feedback→Adjustment→Task 链；今天优先）

## 6. 不计算假进度

零掌握率/成功率/努力分/AI 预测；趋势为真实计数；下一步仅真实已排数据推导。

## 7. Commands（84 → 92）

get_profile_range_sessions/evaluations/tasks、feedbacks_created/resolved、adjustments、
get_learning_trend、get_next_actions。api.ts 同步 +8；types +TrendDay/NextAction。

## 8. Tests（insight_review.rs，6 用例）

Schema 保持 v008 / range 边界+全链 Profile 隔离（含 Today/Week 窗口） /
Stage Review 范围（min(today,end)）/ 30 天趋势单查询正确性（含空日全零） /
Next Action 来源链（Task 完成后移除）/ **完整自我修正闭环**（同一套正式数据：
Profile→Goal→Knowledge→Task→Session→failed→用户确认 Feedback→Adjustment→
重新学习 Task→Session→passed→用户确认 resolve→Review Range→Insight 趋势全对账）。

## 9. Gate 3 结果

- TS 0 error；cargo check 通过；**cargo test 118/118**
  （8+18+5+9+6+8+9+10+15+6+24；期间 Windows 沙箱多次锁定旧测试 exe，touch+删除锁文件后恢复，非测试失败）
- npm run tauri dev：app.exe 正常，`database already up to date (latest v008)`（无 v009）
- 桌面零污染

## 10. BATCH-01 结论

DEV-0013/0014/0015 全部完成，三道 Gate 全过：
自我修正学习闭环（Evaluation→Feedback→Adjustment→Task→再验证→resolve→长期 Review）已打通，
且全部为同一套正式数据（无 Mock / 无第二套模型）。
