# AI 操作记录 0014 - DEV-0014：Adjustment System V1（BATCH-01 第 2/3）

- 时间：2026-08-15（Asia/Shanghai）
- 操作编号：0014
- 关联任务：BATCH-01（Gate 1 通过后自动进入）
- 结果：Gate 2 通过（详见文末）

---

## 1. 任务目标

建立 Adjustment System V1：记录"发现问题后我准备怎么改变下一步学习"，
并保证 Feedback → Adjustment → 正式 Task 的执行链。
最高架构原则：**Adjustment 绝不成为第二套 Task 系统**。

## 2. Migration v008（v008_adjustments.rs）

- `adjustments` 表：feedback_id NOT NULL / goal_id NOT NULL / learning_item_id /
  adjustment_type / title / note DEFAULT '' / status DEFAULT 'planned' / target_date /
  task_id / plan_id / created_at / updated_at / completed_at
- FK：feedback→feedbacks CASCADE；goal→goals CASCADE；learning_item SET NULL；
  task→tasks SET NULL；plan→plans SET NULL
- 索引：feedback_id / goal_id / status；幂等；单一职责
- **实机升级验证：`[migration] applied v008 adjustments` → `database now at v008`**

## 3. Repository（adjustment.rs）

create（Guardrail：Feedback/learning_item/task 必须同 Goal，跨 Profile 拒绝）/ get /
list_by_feedback / list_by_profile / list_pending_by_profile / mark_completed（completed_at）/
cancel / count_by_status_by_profile。status 仅表示调整决策（planned/completed/cancelled）。

## 4. Commands / API（65→75 之后：+9，共 84）

create/get/list×3/mark_completed/cancel/count + **arrange_relearn_adjustment**
（一条命令双记录：① 正式 Task[真正执行对象，出现在对应日期今日任务] ② Adjustment[记录
Feedback→调整→Task 关系]；知识节点跨 Goal 拒绝；人话错误）。
api.ts +10 函数；types.ts Adjustment + 五类型 + 三状态 + 中文标签。

## 5. Feedback → Adjustment UI（FeedbackCard 统一组件，复用 Review/Knowledge）

主按钮 [安排重新学习] + ··· 菜单（增加练习 / 标记已解决 / 忽略），非按钮墙。
RelearnModal：任务标题（默认"重新学习/练习"）/ 安排日期（默认今天）/ 说明；
提交后 arrangeRelearnAdjustment 双记录 + 轻提示（今天→"已安排到今天"）。

## 6. 不自动 resolve

- 创建 Adjustment（安排重新学习）→ Feedback 保持 open（测试断言）
- passed Evaluation → Feedback 仍 open（测试断言）
- Review 新增"这次验证已经通过"区：对应知识有 open 问题 → [标记已解决]（用户确认）/ 暂时保留

## 7. Tests（adjustment_system.rs，8 用例）

v008 应用+幂等 / v007→v008 旧 Feedback 保留 / create 绑定+默认 planned /
跨 Profile Feedback 拒绝 / 跨 Goal item 拒绝 / 安排重新学习双记录
（Task learning_item·planned_date 正确 + Adjustment.task_id 关联）/
Adjustment 与 passed 均不自动 resolve（显式 resolve 才 resolved）/
mark_completed·cancel·pending·计数·Profile 隔离。
旧测试 schema 断言 [1..7]→[1..8]。

## 8. Gate 2 结果

- TS 0 error；cargo check 通过；**cargo test 112/112**（10+9+24+18+15+8+6+5+9+8）
- npm run tauri dev：app.exe 正常，v008 应用成功（v006→v007→v008 链路实机验证完成）
- 数据链验证：arrange_relearn 测试覆盖 Feedback→Adjustment→Task→对应日期 今日任务
- 无 Hard Stop → **自动进入 DEV-0015**
