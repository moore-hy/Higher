# AI 操作记录 0050 - DEV-0049 Human Real-use Validation Fix 01

- 时间：2026-08-16 01:15；Schema v013→**v014**；三件事全部完成，Gate 全绿

## A. Word-like 富文本编辑器（Tiptap）

- 新依赖（任务授权基础组合）：@tiptap/react / @tiptap/pm / @tiptap/starter-kit ^3.30.1
- **v014**（v014_session_rich_document.rs）：仅 `ALTER TABLE study_sessions ADD COLUMN note_document_json TEXT`（幂等：列存在跳过）；实机真实 DB 升级成功，旧 note 原样保留
- 新组件 RichDocEditor.tsx：StarterKit（Paragraph/H2/H3/双列表/CodeBlock）+ 自定义 higherImage/higherVideo Node（attrs 仅 attachmentId/fileName/caption；ReactNodeView 加载 Sandbox 附件；可选中/查看原图/移除引用）
- CodeBlock 复制：装饰层 MutationObserver 向 pre 注入「复制」按钮（contentEditable=false，不影响文档 JSON；克隆 pre 去按钮取全文本→剪贴板，1.5s「已复制」）
- 三路图片入流：Ctrl+V 剪贴板（DOM paste）/ 拖放 Drop Position / dialog 上传 —— 均经现有附件链（addAttachmentFromBase64/addLearningAttachment）→ 光标处插 Node；画图保存→higherImage 插入当前位置；视频同（HTML5 controls 不自动播）
- §3.4 兼容：note_document_json 优先；NULL→noteToDocument(旧 v2 JSON blocks 或纯文本按行转段落)；首次保存才写 document（不回写历史）
- §9 投影：documentToPlainText（正文/CodeBlock 原文；媒体→[图片: 文件名]/[视频: 文件名]）
- §8 附件区改索引：AttachmentList 增「插入正文」；LearningWorkspace 删除附件→window 事件→编辑器同步移除所有引用 Node（选同步移除方案，无破损引用）
- §10 后端：update_session_document command（事务包裹 update_document：note+doc 同一 UPDATE 原子写；Session 不存在 Err；5MB JSON 上限；不触碰 profile/started/ended）
- §9.1 结束顺序保持：flush(两字段) → 成功 → endSession → End Sheet；失败不结束、Session 保持 active

## B. Session 学习日统一 UTC+8

- **根因**：started_at 以 datetime('now')=UTC 存储，查询/聚合裸用 date(started_at)=UTC 日；本地 00:05（UTC 前一日 16:05）被归前一天
- **修法（不改任何历史数据）**：全链统一 `date(x, '+8 hours')`——
  study_session.rs（range 查询）/ mod.rs day_detail（sessions+evaluations）/ study_profile.rs get_calendar（sessions+evaluations 两段）/ insight.rs（趋势 days 标签生成 + sessions/evaluations/feedbacks/adjustments 四 CTE + 月活跃天）/ ai/context.rs（daily_block sessions+evaluations、recent_sessions、item notes）/ task.rs（list_today 两处"今天"）/ evaluation.rs range / feedback.rs、adjustment.rs range / cleanup.rs（sessions/evaluations 的今天/本月/今年定位 + session 附件收集）
- 前端：utils.ts 新增 studyDayOf()（UTC→UTC+8 学习日，注释禁裸 slice）；PlanningCalendar 月历聚合、Planning dayLabel（最近学习）改用之
- 测试先行：batch049.rs §11.3 四 Case（00:01/00:05/23:59/次日 00:01）+ day_detail 一致 + daily_review 与 Date Detail 同数据

## C. Date Detail 当天摘要

- PlanningCalendar 抽屉标题下新增 summary 行：学习时间（无则"暂无"）/学习记录 N 次/任务 x/y + 完成率（total>0 才显示，round 百分比）；无任务→「任务 暂无安排」（不显示 0%）
- 数据源：dayDetail.total_seconds/sessions.length/tasks（沿用既有）；任务/学习记录/验证明细区全部保留

## Gate

- tsc 0 / cargo check 0/0
- cargo test：**198 passed / 0 failed**；ENV_BLOCKED=ai_panel(8)、evaluation_workflow(5)（SAC 拦截本轮新编译 exe，单次确认未重试；同源码逻辑此前批次通过，非业务失败；建议下次自然重编译复跑确认）
- tauri dev：`applied v014 session_rich_document` 实机升级成功，无 panic；真实 DB 204KB 完好；Desktop 0 污染
- Knowledge.tsx / KnowledgeFlow.tsx 零触碰（时间戳仍为 BATCH-04 施工时刻）

## 测试新增（batch049.rs，10 用例）

日期四边界 / day_detail 日期一致+摘要数据源 / daily_review 与 Date Detail 同数据 /
v013→v014 迁移保 note 且 doc=NULL / update_session_document 原子+守卫（不存在 Err/5MB 拒绝/不触碰时间）/ 新库直达 v014 / note 投影含媒体占位 / started_at 仍 UTC 格式
（另：14 个旧测试文件的版本断言 13→14 与 today_str/current_utc_datetime 测试助手改 +8h 学习日语义）
