# DEV-0065.1 · Higher Desktop Shell
## Custom Desktop Titlebar + Two-State Higher AI Rail
### Decision-Complete Implementation Task

> **Authority**
>
> - User = Product Owner
> - ChatGPT = Product / Architecture Decision Owner
> - Trae = Implementation Engineer
>
> Trae has **no product decision authority**.
>
> This task is based on a fresh full-project audit of the current Higher worktree uploaded on 2026-08-22.
> Do not infer current state from old TASK / old ENVIRONMENT / old chat history when source differs.
>
> If real source conflicts with this task in a way that cannot be mechanically resolved:
>
> ```text
> STOP
> SOURCE_CONFLICT
> ```
>
> Report facts only. Do not redesign.

---

# 0. DEV Goal

This DEV turns Higher from a WebView that still visibly uses the Windows native white title bar into a coherent desktop application shell.

It also simplifies Higher AI from the current three-state model:

```text
Expanded
Collapsed Rail
Closed + FAB
```

to the final two-state model:

```text
Expanded
⇄
Collapsed Rail
```

There is no user-visible “Closed” state after this DEV.

This DEV contains exactly two product changes:

```text
A. Custom Desktop Titlebar
B. Higher AI Two-State Rail
```

No learning-domain or AI-runtime behavior is redesigned.

---

# 1. User Product Decision

## 1.1 One visual surface from top to bottom

The user has already accepted the Single Global Wallpaper system.

The wallpaper must remain ONE continuous image:

```text
top custom titlebar
↓
left sidebar
↓
main
↓
right Higher AI
↓
bottom
```

The titlebar must NOT own another background image.

It only uses a translucent Higher surface so that the existing global wallpaper remains visible underneath.

Changing wallpaper / wallpaper strength / saturation / dimming must automatically change the titlebar appearance because it is looking through the same global wallpaper.

---

# 2. Current Source Audit Snapshot

## 2.1 Git truth at audit time

```text
HEAD:
b14e23703999d5855ef020e6b27a1684ada03741

Branch:
main
```

Worktree is intentionally DIRTY from DEV-0063 / DEV-0064R / DEV-0064R.2.

Do not reset it.

---

## 2.2 Project stack

Frontend:

```text
React 19
TypeScript
Vite 8
react-router-dom 7
@tauri-apps/api 2.11.1
```

Desktop:

```text
Tauri Rust 2.11.3
Tauri CLI 2.11.4
```

Current app window is NOT created from `tauri.conf.json`.

Current:

```json
"app": {
  "windows": []
}
```

Main window is dynamically created in:

```text
src-tauri/src/lib.rs
run()
WebviewWindowBuilder::new(...)
```

Current builder:

```rust
.title("Higher")
.inner_size(1024.0, 720.0)
.resizable(true)
```

Therefore the correct source of truth for removing native window decoration is:

```text
src-tauri/src/lib.rs
```

NOT a second window definition in `tauri.conf.json`.

---

# 3. Current Desktop Window Truth

Current dynamic window:

```text
label = main
title = Higher
inner size = 1024 × 720
resizable = true
native decorations = currently default ON
```

This is the reason Windows still draws the white titlebar visible in Human Runtime.

---

# 4. Current Capability Truth

Current:

```json
"permissions": [
  "core:default",
  "dialog:default",
  "notification:default"
]
```

`core:default` includes read/query window defaults but does NOT include all frontend window mutation commands required by a custom titlebar.

This DEV must explicitly add:

```text
core:window:allow-close
core:window:allow-minimize
core:window:allow-toggle-maximize
core:window:allow-start-dragging
```

Do NOT grant:

```text
shell
process
filesystem broad write
admin
registry
global shortcut
```

No permission expansion beyond the four exact window permissions above.

---

# 5. Current Wallpaper Truth

Current App root:

```tsx
<ActiveProfileProvider>
  <WallpaperLayers />
  <ProfileGate />
</ActiveProfileProvider>
```

`WallpaperLayers` is already correct:

```text
fixed
inset: 0
pointer-events: none
one real wallpaper image layer
one dimming layer
```

Do NOT rebuild this system.

Do NOT introduce a titlebar background image.

Do NOT duplicate `--h-wallpaper-image`.

---

# 6. Current Higher AI Truth

Current `AiPanel.tsx` has THREE states.

State 1:

```text
open = false
→ .aipanel-fab
→ bottom-right “✨ AI”
```

State 2:

```text
open = true
collapsed = true
→ 46px rail
```

State 3:

```text
open = true
collapsed = false
→ 340px expanded panel
```

Persistence is currently split between:

```text
DB UI setting:
ui.ai_panel_open

localStorage:
higher.aiPanel.mode
```

This split is no longer needed after the product decision.

---

# 7. Final Higher AI Product Model

After DEV-0065.1:

```text
Higher AI always exists in App Shell.
```

Only:

```text
Expanded
Collapsed
```

exist.

No:

```text
Closed
FAB
X close button
```

---

# 8. AI State Persistence Decision

The only active visual-state preference after this DEV is:

```text
localStorage:
higher.aiPanel.mode
```

Allowed values:

```text
expanded
collapsed
```

Fresh install / missing value:

```text
collapsed
```

Reason:

> AI remains immediately available but does not overpower the learning workspace by default.

Existing saved values:

```text
expanded → preserve
collapsed → preserve
```

Legacy:

```text
ui.ai_panel_open
```

must NOT be migrated, deleted, rewritten, or interpreted by new UI.

It may remain in the existing settings table as stale backwards-compatible data.

Schema change:

```text
0
```

Migration:

```text
0
```

Backend setting command:

```text
unchanged
```

---

# 9. AI Runtime Freeze

This DEV may change AI shell visibility only.

Absolutely frozen:

```text
aiStartRun
aiCancelRun

Conversation create/list/archive
Conversation IDs
Message history
Streaming

ai://delta
ai://source
ai://changeset
ai://run-status
ai://error
ai://applied

Pending Action
Grounding
SemanticAction
Turn Interpreter
Provider
Compatibility
ChangeSet
Proposal
Approval First
Apply
Model selector
Enter send
Shift+Enter newline
```

AI Direct Write:

```text
0
```

---

# 10. Custom Titlebar Architecture

Create:

```text
src/components/DesktopTitlebar.tsx
```

App structure becomes:

```text
ActiveProfileProvider
├── WallpaperLayers
├── DesktopTitlebar
└── app-shell__content
    └── ProfileGate
```

Exact conceptual JSX:

```tsx
<ActiveProfileProvider>
  <WallpaperLayers />
  <DesktopTitlebar />
  <div className="app-shell__content">
    <ProfileGate />
  </div>
</ActiveProfileProvider>
```

Titlebar exists during ALL ProfileGate phases:

```text
loading
no_profiles
select
active
```

This prevents native-looking shell changes during startup/profile selection.

---

# 11. Desktop Titlebar Height

Global token:

```css
--h-titlebar-height: 34px;
```

Locked:

```text
34px
```

Trae may not choose 30 / 32 / 36 / 40.

---

# 12. App Content Geometry

Global wallpaper remains:

```text
fixed inset 0
```

It is NOT offset.

App UI content is offset.

Required:

```css
.app-shell__content {
  height: calc(100vh - var(--h-titlebar-height));
  margin-top: var(--h-titlebar-height);
  overflow: hidden;
}
```

Existing:

```css
.layout {
  height: 100vh;
}
```

must become:

```css
.layout {
  height: 100%;
}
```

ProfileGate also must not reserve another full viewport underneath the 34px titlebar.

Existing:

```css
.profile-gate {
  min-height: 100vh;
}
```

becomes:

```css
.profile-gate {
  min-height: 100%;
}
```

Do NOT solve this by scattering:

```text
padding-top: 34px
```

into Today / Planning / Knowledge / Settings / LearningWorkspace.

Titlebar geometry belongs to App Shell only.

---

# 13. Native Decoration Removal

Current dynamic builder is the single window source.

Change:

```rust
.resizable(true);
```

to:

```rust
.resizable(true)
.decorations(false);
```

Do NOT create a second main window in:

```text
tauri.conf.json
```

`app.windows` remains:

```json
[]
```

---

# 14. Forbidden Window Options

Do NOT add:

```text
transparent(true)
fullscreen(true)
always_on_top(true)
skip_taskbar(true)
shadow(false)
visible(false)
```

This DEV is NOT a transparent OS window.

Higher wallpaper is a WebView UI background, not Windows desktop transparency.

---

# 15. Desktop Titlebar Visual

Base:

```text
height 34px
position fixed
top 0
left 0
right 0
```

Titlebar background:

```css
background: var(--h-sidebar);
```

Because `--h-sidebar` is already wallpaper-aware:

Without wallpaper:

```text
solid Higher dark sidebar surface
```

With wallpaper:

```text
translucent Higher dark surface
→ same global wallpaper visible underneath
```

Do NOT use:

```text
background-image
```

in titlebar.

---

# 16. Titlebar Z Layer

Locked:

```text
desktop titlebar z-index = 1200
```

Current notable layers:

```text
Task menu 100
AI panel 200
Modal overlay 900
Modal 910
Toast 1000
Desktop titlebar 1200
```

Reason:

> Window controls must remain available and visually stable above page/modals.

---

# 17. Titlebar Border

Use only:

```text
subtle bottom border
```

via existing:

```text
--h-border
```

No decorative glow.

No thick frame.

No neon.

No extra glass blur.

---

# 18. Titlebar Layout

Structure:

```text
[ app identity / drag zone                         ][ — ][ □ ][ × ]
```

The center/left region is the drag region.

Controls are outside the drag region.

Locked titlebar horizontal behavior:

```text
left drag region = flex: 1
controls = max-content
```

---

# 19. Titlebar App Identity

Use:

```text
Higher
```

in a restrained style.

No subtitle.

No current page title.

No profile name.

No breadcrumbs.

No date.

The app already has page-specific hierarchy below.

---

# 20. Titlebar Controls

Exactly three:

```text
Minimize
Toggle Maximize / Restore
Close
```

No settings button.

No pin.

No menu.

No AI button.

---

# 21. Window API

Use:

```ts
import { getCurrentWindow } from "@tauri-apps/api/window";
```

Current window:

```ts
const appWindow = getCurrentWindow();
```

Actions:

```text
minimize → appWindow.minimize()
maximize/restore → appWindow.toggleMaximize()
close → appWindow.close()
```

Do not create a new Window instance.

Do not target a guessed label from React.

---

# 22. Drag Region

Primary implementation:

```text
Tauri drag region
```

Use:

```text
data-tauri-drag-region
```

on the actual drag region.

Because Tauri drag-region behavior applies to the actual annotated element, keep interactive controls OUTSIDE it.

If the drag region contains noninteractive child text, annotate the relevant noninteractive child too if required by source behavior.

Do NOT place:

```text
minimize
maximize
close
```

inside the drag-region node.

---

# 23. Double Click

Required Human Runtime behavior:

```text
double click drag region
→ toggle maximize / restore
```

Prefer the native Tauri drag-region behavior.

Do NOT add a second manual maximize handler unless real source/runtime proves the native drag-region path cannot provide the required behavior.

If Human Runtime fails:

```text
STOP
TITLEBAR_DOUBLE_CLICK_RUNTIME
```

Do not silently stack duplicate handlers.

---

# 24. Window Control Icons

Use inline CSS/SVG/simple DOM.

No new icon package.

No remote assets.

No CDN.

No font file.

Controls:

```text
minimize = horizontal line
maximize = square
close = X
```

If implementing restore-state visual:

```text
maximized → double-square restore icon
normal → single-square maximize icon
```

This is allowed but must use existing Tauri `isMaximized()` / resize observation only.

It must NOT add another dependency.

If accurate icon-state synchronization becomes nontrivial:

```text
keep a neutral square toggle icon
```

Behavior correctness has priority over decorative state.

---

# 25. Window Control Hover

Minimize / maximize:

```text
transparent default
subtle neutral hover
```

Close:

```text
transparent default
red hover
white icon on red hover
```

This should feel like desktop software, not a web CTA.

---

# 26. Browser/Vite Preview Safety

Higher still supports browser preview/dev inspection.

`DesktopTitlebar` may render in browser preview, but Tauri window commands must not execute outside Tauri.

Reuse:

```text
isTauriRuntime()
```

from:

```text
src/utils/tauriEnv.ts
```

No console-error spam.

No unhandled Promise rejection.

No fake window behavior in browser.

---

# 27. Window Control Error Rule

Tauri runtime control failures must not be silently converted into fake success.

For automated implementation:

```text
await command
```

Failure may be caught only to prevent unhandled rejection.

Human Runtime is authoritative.

If any of:

```text
minimize
maximize
close
drag
```

does nothing in the real desktop app:

```text
FAIL
```

Do not declare PASS because TypeScript/build passed.

---

# 28. Windows Maximize Semantics

Use:

```text
maximize
```

NOT:

```text
fullscreen
```

Expected:

```text
Windows taskbar remains respected
```

When maximized:

```text
Higher fills Windows work area
taskbar remains available
```

Human Runtime required.

---

# 29. Window Resize Semantics

Existing:

```text
resizable(true)
```

must remain.

Human Runtime must verify:

```text
resize from window edges/corners
```

after decorations are removed.

If borderless runtime removes resizing unexpectedly:

```text
STOP
WINDOW_RESIZE_RUNTIME
```

Do not invent custom resize grips in this DEV unless explicitly approved.

---

# 30. AI Panel: Remove Closed State

Delete user-visible Closed branch:

```tsx
if (!open) {
  return <button className="aipanel-fab">...</button>;
}
```

Delete:

```text
X Close button
```

Delete CSS:

```text
.aipanel-fab
.aipanel-fab:hover
```

No replacement FAB.

---

# 31. AiPanelContext Simplification

Remove UI-shell state from `AiPanelState`:

```text
open
setOpen
```

Remove provider state:

```text
const [open, setOpenState]
```

Remove:

```text
ui.ai_panel_open
```

load/store logic from `AiPanelContext.tsx`.

Remove `setOpenState(true)` from:

```text
sendChat
runAction
```

because Higher AI always exists.

Do NOT remove generic APIs from:

```text
src/api.ts
```

`getUiSetting` / `setUiSetting` are still used by other features.

Do NOT alter backend setting allowlists.

---

# 32. Layout Simplification

Current:

```tsx
const { open: aiOpen, setPageContext } = useAiPanel();
```

becomes:

```tsx
const { setPageContext } = useAiPanel();
```

Current:

```tsx
<main className={"layout__main" + (aiOpen ? " layout__main--with-ai" : "")}>
```

becomes:

```tsx
<main className="layout__main">
```

The AI Panel remains in normal flex layout.

Main width automatically follows:

```text
340px expanded
46px collapsed
```

No JS width calculation.

---

# 33. AI Two-State Initialization

Use:

```text
higher.aiPanel.mode
```

Initialization:

```text
"expanded" → expanded
"collapsed" → collapsed
missing/invalid → collapsed
```

Write only:

```text
expanded
collapsed
```

---

# 34. Whole AI Rail Click Target

Current rail only exposes a small 30px expand button.

Final behavior:

> The ENTIRE 46px rail is clickable.

Preferred semantic structure:

```tsx
<aside className="aipanel aipanel--rail">
  <button className="aipanel__rail-hit" ...>
    <span>icon</span>
    <span>HIGHER AI</span>
  </button>
</aside>
```

The button fills:

```text
width 100%
height 100%
```

It must support:

```text
mouse click
keyboard Enter
keyboard Space
```

automatically via native button semantics.

No nested button.

---

# 35. AI Rail Hover

Whole rail hover:

```text
very light accent-soft / surface highlight
```

Do not make the rail bright purple.

The wallpaper must remain visible.

---

# 36. AI Rail Visual

Keep:

```text
46px
vertical HIGHER AI
small ✦ identity mark
```

The existing visual direction is accepted.

Do not redesign to a floating circle.

---

# 37. Expanded AI Header

Final actions:

```text
History
New Conversation
Collapse
```

No:

```text
Close X
```

Exact visible action count:

```text
3
```

---

# 38. Collapse Behavior

Click:

```text
Collapse
```

→

```text
setMode(collapsed)
localStorage = collapsed
```

No:

```text
conversation reset
message reload
network call
AI cancellation
proposal loss
```

---

# 39. Expand Behavior

Click anywhere on rail:

```text
setMode(expanded)
localStorage = expanded
```

No:

```text
new Conversation
network call
AI call
message reload
```

Current in-memory AI state remains exactly as it was.

---

# 40. Auto Expand Behavior

Preserve existing behavior:

```text
page AI action starts
→ expand

higher:aipanel-pending-send
→ expand
```

Do not change the event name.

Do not modify AI send semantics.

---

# 41. Cross-Page AI Mode

Navigate:

```text
Today
Planning
Knowledge
Data
Settings
LearningWorkspace
```

Collapsed remains collapsed unless an AI-trigger event explicitly expands it.

Expanded remains expanded.

Route navigation itself does NOT force a mode change.

---

# 42. Conversation Persistence

Collapsed → Expanded:

```text
same conversationId
same loaded messages
same input where technically retained
same pending ChangeSet
same current run
```

No state recreation from mounting/unmounting.

Therefore:

```text
AiPanel component must remain mounted
```

both expanded and collapsed.

Conditional render inside AiPanel is allowed.

Conditional mount/unmount of AiPanel from Layout is forbidden.

---

# 43. Streaming While Collapsed

If a run is active and user collapses panel:

```text
run continues
stream listeners remain alive
conversation remains alive
```

Do not cancel just because UI is collapsed.

Existing event listeners stay mounted because AiPanel itself stays mounted.

---

# 44. Single Global Wallpaper Invariant

After adding titlebar, total production wallpaper image consumers remain:

```text
1
```

Settings Preview remains the only UI preview exception.

Titlebar:

```text
0 wallpaper image declarations
```

AI Rail:

```text
0 wallpaper image declarations
```

They only consume translucent tokens.

---

# 45. No Backdrop Blur

Do NOT add:

```text
backdrop-filter
blur()
```

The accepted visual principle is:

```text
darkened wallpaper
+
translucent surfaces
```

not frosted-glass blur.

---

# 46. Allowed Source Files

This DEV may modify:

```text
src/App.tsx
src/Layout.tsx

src/components/DesktopTitlebar.tsx          NEW
src/components/ai/AiPanel.tsx
src/components/ai/AiPanelContext.tsx

src/styles.css

src-tauri/src/lib.rs
src-tauri/capabilities/default.json

src-tauri/tests/batch064_ui.rs
src-tauri/tests/batch0651_ui.rs             NEW

.higher/TRAE_RUN.md
.higher/ENVIRONMENT.md
```

If a source-contract test genuinely requires a small matching update:

```text
src-tauri/tests/batch064r2_ui.rs
```

is allowed only if its old assertion is directly contradicted by DEV-0065.1.

Expected:

```text
normally no change needed
```

---

# 47. Forbidden Source Files

Do NOT modify:

```text
src/api.ts
src/types.ts

src/pages/Today.tsx
src/pages/Planning.tsx
src/pages/Knowledge.tsx
src/pages/Data.tsx
src/pages/Settings.tsx
src/pages/LearningWorkspace.tsx

src/components/TaskModal.tsx
src/components/DailyTasksSection.tsx
src/components/ChangeSetReview.tsx
src/components/KnowledgeFlow.tsx
src/components/RichDocEditor.tsx
src/components/PlanningWeekBoard.tsx

src/appearance/**

src-tauri/src/ai/**
src-tauri/src/repository/**
src-tauri/src/migrations/**
src-tauri/src/db.rs

package.json
package-lock.json
Cargo.toml
Cargo.lock
tauri.conf.json
```

Important:

```text
tauri.conf.json windows = []
```

must stay as-is.

---

# 48. Why `tauri.conf.json` Is Frozen

Current app creates `main` dynamically in Rust.

Adding another main window in config risks:

```text
duplicate window
different source of truth
dev/prod divergence
```

Therefore:

```text
Window decoration change lives in Rust builder only.
```

---

# 49. Dependency Freeze

New npm dependencies:

```text
0
```

New Rust dependencies:

```text
0
```

New icon dependency:

```text
0
```

---

# 50. Backend / DB Freeze

Schema:

```text
v024 → v024
```

Migration:

```text
0
```

Repository behavior:

```text
0
```

AI Runtime:

```text
0
```

---

# 51. Current Worktree Safety

Before work Trae must record:

```powershell
cd "C:\Users\37653\Desktop\Higher"

git rev-parse HEAD
git branch --show-current
git status --short
git diff --name-only
git ls-files --others --exclude-standard
Get-Date -Format "yyyy-MM-ddTHH:mm:sszzz"
```

Expected HEAD:

```text
b14e23703999d5855ef020e6b27a1684ada03741
```

Expected branch:

```text
main
```

The current modified/untracked DEV-0063/0064 files are known.

Do NOT:

```text
reset
restore
clean
checkout .
stash
```

Unknown source modifications outside known worktree:

```text
STOP
UNKNOWN_WORKTREE_DIFF
```

---

# 52. Phase A · Source Audit Before Editing

Trae reads:

```text
WORKING_RULES
ENVIRONMENT
TRAE_RUN
this TASK

src/App.tsx
src/Layout.tsx
src/components/ai/AiPanel.tsx
src/components/ai/AiPanelContext.tsx
src/components/WallpaperLayers.tsx
src/styles.css

src-tauri/src/lib.rs
src-tauri/capabilities/default.json
src-tauri/tauri.conf.json

src-tauri/tests/batch064_ui.rs
src-tauri/tests/batch064r2_ui.rs
src-tauri/tests/ai_panel.rs
```

Then write a short Source Evidence block to TRAE_RUN before editing.

---

# 53. Phase B · AI Two-State Refactor First

Do AI shell before changing native titlebar.

Reason:

```text
pure frontend
easy regression boundary
```

Actions:

```text
remove closed branch
remove FAB
remove X
remove open/setOpen context
default missing mode = collapsed
whole rail clickable
```

No AI runtime edits.

---

# 54. Phase B Gate

Run:

```powershell
npx tsc --noEmit
npm run build
```

Then source audit:

```text
AiPanel.tsx contains no title="关闭"
AiPanel.tsx contains no aipanel-fab
AiPanel.tsx contains no setOpen(false)
AiPanelContext.tsx does not load ui.ai_panel_open
higher.aiPanel.mode remains
ai:// event strings remain
aiStartRun remains
ChangeSetReview remains
```

---

# 55. Phase C · Desktop Titlebar

Then:

```text
DesktopTitlebar.tsx
App shell wrapper
CSS geometry
decorations(false)
capabilities
```

Do not mix business UI changes.

---

# 56. Phase C Gate

Run:

```powershell
npx tsc --noEmit
npm run build

cd src-tauri
cargo check -j 1
```

If capability schema validation fails:

```text
STOP
TAURI_CAPABILITY_CONFLICT
```

Do not weaken permissions.

---

# 57. New Test Suite

Create:

```text
src-tauri/tests/batch0651_ui.rs
```

Source-contract test only.

No Provider.

No network.

No DB mutation needed.

---

# 58. T01 Dynamic Window Remains

Assert:

```text
tauri.conf app.windows = []
```

and:

```text
WebviewWindowBuilder::new
```

still creates main.

---

# 59. T02 Decorations Off

Assert `lib.rs` builder contains:

```text
decorations(false)
```

in the main-window builder chain.

---

# 60. T03 No Transparent OS Window

Assert main-window builder does NOT contain:

```text
transparent(true)
fullscreen(true)
always_on_top(true)
```

---

# 61. T04 Exact Window Permissions

Assert capability includes:

```text
core:window:allow-close
core:window:allow-minimize
core:window:allow-toggle-maximize
core:window:allow-start-dragging
```

Do not assert unrelated permission changes.

---

# 62. T05 DesktopTitlebar At App Root

Assert App contains:

```text
<WallpaperLayers />
<DesktopTitlebar />
app-shell__content
<ProfileGate />
```

Wallpaper remains root sibling, not moved inside Layout.

---

# 63. T06 Titlebar Is Not Wallpaper Consumer

Assert DesktopTitlebar/CSS contains no:

```text
--h-wallpaper-image
background-image
```

for desktop titlebar selectors.

---

# 64. T07 Titlebar Geometry

Assert:

```text
--h-titlebar-height: 34px
position: fixed
top: 0
left: 0
right: 0
height: var(--h-titlebar-height)
z-index: 1200
```

---

# 65. T08 Content Offset

Assert:

```text
app-shell__content
margin-top titlebar height
height calc(100vh - titlebar height)
```

and:

```text
.layout height = 100%
.profile-gate min-height = 100%
```

---

# 66. T09 Window Controls

Assert DesktopTitlebar imports:

```text
getCurrentWindow
```

and invokes:

```text
minimize
toggleMaximize
close
```

---

# 67. T10 Drag Region

Assert titlebar has:

```text
data-tauri-drag-region
```

and controls are structurally separate.

---

# 68. T11 AI No Closed State

Assert AiPanel contains no:

```text
aipanel-fab
title="关闭"
setOpen(false)
if (!open)
```

---

# 69. T12 AI Only Two Modes

Assert:

```text
higher.aiPanel.mode
expanded
collapsed
```

and missing/invalid initialization resolves to collapsed.

---

# 70. T13 Legacy Open UI State Not Consumed

Assert `AiPanelContext.tsx` does NOT contain:

```text
ui.ai_panel_open
```

Do NOT assert backend no longer supports that key.

---

# 71. T14 Whole Rail Clickable

Assert:

```text
aipanel__rail-hit
```

exists as one full-rail interactive control.

Rail must not contain a tiny separate nested expand button as the only target.

---

# 72. T15 Expanded Header Has Three Actions

Source contract should verify these remain:

```text
历史对话
新对话
收起为侧栏
```

and Close is absent.

---

# 73. T16 AI Runtime Frozen

Assert AiPanel still contains:

```text
aiStartRun
aiCancelRun

ai://delta
ai://source
ai://changeset
ai://run-status
ai://error
ai://applied
```

---

# 74. T17 Proposal Runtime Frozen

Assert:

```text
ChangeSetReview
```

still present.

---

# 75. T18 Send Semantics Frozen

Assert:

```text
e.key === "Enter" && !e.shiftKey
```

and Shift+Enter UI hint remains.

---

# 76. T19 No New Wallpaper Consumer

Reuse DEV-0064R.2 invariant:

Production wallpaper image consumer count unchanged.

---

# 77. T20 No New Dependencies

Source-contract / git diff check:

```text
package.json
package-lock.json
Cargo.toml
Cargo.lock
```

have no DEV-0065.1 delta.

---

# 78. Update Existing batch064 U21

Current batch064 U21 intentionally asserts old three-state semantics:

```text
ui.ai_panel_open preserved
collapse + close separated
```

This is now stale due explicit Product Owner decision.

Update U21 to new truth:

```text
Collapsed Rail exists
higher.aiPanel.mode exists
expanded/collapsed exists
Close absent
FAB absent
```

Do not weaken unrelated U22/U23 AI runtime/proposal assertions.

---

# 79. Automated Gate

Required:

```powershell
cd "C:\Users\37653\Desktop\Higher"

npx tsc --noEmit
npm run build

cd src-tauri

cargo check -j 1

$env:RUST_TEST_THREADS='1'

cargo test --test batch0651_ui -j 1
cargo test --test batch064_ui -j 1
cargo test --test batch064r2_ui -j 1
cargo test --test batch063_ui -j 1

cargo test --test ai_panel -j 1

cargo test --test batch062r1 -j 1
cargo test --test batch062r -j 1
cargo test --test batch062 -j 1
cargo test --test batch061r -j 1
cargo test --test batch0602 -j 1
```

No real Provider calls.

---

# 80. Optional Compile Gate

If Windows Smart App Control allows:

```text
cargo test --no-run
```

may be run.

If blocked by OS 4551:

```text
ENV_BLOCKED_SAC
```

per project Working Rules.

Do not change Windows security settings.

---

# 81. Automated Gate Status Language

Trae may only report:

```text
DEV-0065.1
AUTOMATED GATE PASSED
HUMAN RUNTIME PENDING
```

Automated tests cannot prove borderless Windows UX.

---

# 82. Human Runtime H01 · Startup

Completely close Higher.

Open again.

Expected:

```text
no Windows white native titlebar
Higher custom titlebar visible
wallpaper reaches the very top
```

No flash of permanent white titlebar after startup.

A brief OS-created frame during process launch is not automatically failure; persistent white titlebar is failure.

---

# 83. H02 · Wallpaper Continuity

Use wallpaper with recognizable landmark.

Observe:

```text
custom titlebar
sidebar
main
AI
```

Expected:

```text
one continuous image coordinate system
```

No titlebar-specific restarted wallpaper.

---

# 84. H03 · No Wallpaper

Settings → restore default/remove wallpaper.

Expected titlebar:

```text
normal Higher dark surface
```

Not:

```text
transparent to Windows desktop
white
gray OS bar
```

---

# 85. H04 · Window Drag

Drag custom titlebar empty area.

Expected:

```text
Higher window moves normally
```

No page text selection.

No button accidental trigger.

---

# 86. H05 · Double Click

Double click titlebar drag region.

Expected:

```text
normal → maximized
maximized → restored
```

---

# 87. H06 · Minimize

Click:

```text
—
```

Expected:

```text
Higher minimizes to taskbar
```

Restore from taskbar.

All state remains.

---

# 88. H07 · Maximize

Click maximize.

Expected:

```text
fills Windows work area
does not become fullscreen
Windows taskbar remains respected
```

Click again:

```text
restore previous window size
```

---

# 89. H08 · Close

Click:

```text
×
```

Expected:

```text
Higher process/window closes normally
```

Restart:

```text
data
wallpaper
AI conversation
```

remain as existing persistence semantics allow.

---

# 90. H09 · Resize

In normal restored mode:

```text
drag left/right/top/bottom/corners
```

Expected:

```text
window remains resizable
```

No custom resize grip in this DEV.

---

# 91. H10 · AI Header

Expanded AI header must show:

```text
History
New Conversation
Collapse
```

Expected:

```text
X absent
```

---

# 92. H11 · AI Collapse

Click collapse.

Expected:

```text
340px panel → 46px rail
Main immediately gets more width
no FAB
no AI runtime call
```

---

# 93. H12 · Whole Rail Expand

Click:

```text
top icon
middle empty rail area
HIGHER AI label area
lower empty rail area
```

Any normal point within the rail’s clickable button should expand.

At minimum one screenshot/manual test must confirm the rail is not only a 30px icon hit target.

---

# 94. H13 · AI Conversation Preservation

Before collapse, note current visible message.

Collapse.

Expand.

Expected:

```text
same Conversation
same messages
same Proposal card if any
```

No reload/new conversation side effect.

---

# 95. H14 · AI Mode Persistence

Set:

```text
collapsed
```

Close Higher.

Reopen.

Expected:

```text
collapsed
```

Set:

```text
expanded
```

Close/reopen.

Expected:

```text
expanded
```

Fresh/missing localStorage is not required to be simulated manually if source-contract test locks default collapsed.

---

# 96. H15 · Page Navigation While Collapsed

Collapse AI.

Navigate:

```text
Today
Planning
Knowledge
Data
Settings
```

Expected:

```text
AI remains collapsed
```

No route forces open.

---

# 97. H16 · Page AI Action Auto Expand

Collapse AI.

Trigger an existing page-level AI action.

Expected:

```text
rail expands
existing action executes through original runtime
```

No new runtime path.

---

# 98. H17 · AI Send

Expanded AI.

Send:

```text
1+1等于多少？只回答数字。
```

Expected:

```text
normal send
normal response
```

This is a Runtime smoke test only.

---

# 99. H18 · Proposal Smoke Test

Use existing TEST-STABLE or another safe task.

Generate a real duration update Proposal.

Expected:

```text
real ChangeSet
Apply-before formal data unchanged
Diff only real changed field
```

This proves shell changes did not regress AI.

Do not repeat the old long-conversation anomaly investigation in this DEV.

---

# 100. H19 · Modal + Titlebar

Open Task Edit Modal.

Expected:

```text
custom titlebar remains visible
modal remains usable
titlebar does not overlap modal content
wallpaper system remains correct
```

---

# 101. H20 · 1024×720

Test original window size.

Expected:

```text
titlebar 34px
sidebar usable
AI expanded usable
main not horizontally broken
```

---

# 102. H21 · 1366×768

Test.

Expected:

```text
no titlebar overlap
no dead controls
```

---

# 103. H22 · 1920×1080

Test.

Expected:

```text
normal large-screen layout
```

---

# 104. H23 · Narrow Width Existing Overlay Rule

Current CSS uses AI absolute overlay at <=1100px.

Do NOT redesign this breakpoint.

Verify:

```text
expanded AI still behaves acceptably
collapsed rail still reachable
```

If current existing <=1100 overlay behavior conflicts structurally with the new titlebar:

```text
STOP
NARROW_SHELL_CONFLICT
```

Do not redesign responsive AI in this DEV.

---

# 105. H24 · Browser Preview

Optional Vite browser preview.

Expected:

```text
page renders
no repeated Tauri invoke error spam
```

Window controls are desktop-only.

Browser preview is not expected to minimize/close browser window.

---

# 106. P2 Issue Recorded, Not Fixed Here

Human Runtime previously observed:

> When navigating Planning Week previous/next, lower selected-day detail may keep the old selected date.

This is a real P2 Planning UX issue.

It is NOT part of DEV-0065.1.

Do not fix it in this task.

Record only.

---

# 107. AI Old-Conversation Anomaly Recorded, Not Fixed Here

Previously one long conversation returned:

```text
no_changeset
```

for an otherwise valid task edit, while a new Conversation generated the correct ChangeSet.

This is not proven to be a shell issue.

Do not modify Runtime in DEV-0065.1.

Record:

```text
P2 runtime investigation later
```

only.

---

# 108. Packaging Boundary

The user also wants:

```text
installer
desktop shortcut
double-click launch
app icon
start menu
uninstall
```

That is:

```text
DEV-0065.2
```

Do NOT build installer behavior in DEV-0065.1.

Reason:

> First prove the borderless desktop shell is stable. Then package that stable shell.

---

# 109. Installer Existing Facts

Current `tauri.conf.json` already has:

```text
bundle.active = true
targets = all
icon.ico / icon.png / platform icons
productName = Higher
identifier = com.higher.desktop
```

This is useful for DEV-0065.2, but packaging is frozen here.

---

# 110. Files Expected To Be Added

Expected:

```text
src/components/DesktopTitlebar.tsx
src-tauri/tests/batch0651_ui.rs
```

Task file itself may also be stored under `.higher/` by the user/Trae workflow.

---

# 111. Expected Modified Files

Expected:

```text
src/App.tsx
src/Layout.tsx
src/components/ai/AiPanel.tsx
src/components/ai/AiPanelContext.tsx
src/styles.css

src-tauri/src/lib.rs
src-tauri/capabilities/default.json
src-tauri/tests/batch064_ui.rs

.higher/TRAE_RUN.md
.higher/ENVIRONMENT.md
```

If more product source files appear:

```text
STOP
SCOPE_EXPANSION_REQUIRED
```

---

# 112. Forbidden Worktree Operations

Do NOT:

```text
git reset
git reset --hard
git restore .
git checkout .
git clean
git stash
```

No commit.

No push.

No tag.

---

# 113. Git

Final:

```text
Git Commit: NO
Git Push: NO
```

User/ChatGPT will decide archival after Human Runtime.

---

# 114. TRAE_RUN Required Evidence

Append:

```text
DEV-0065.1 · Higher Desktop Shell

Timestamp Source:
SYSTEM

Baseline HEAD:
b14e237...

Branch:
main

Known Worktree Before:
...

Source Truth:
- window created dynamically in lib.rs
- tauri.conf windows = []
- native decorations currently on
- capability lacks explicit mutation permissions
- wallpaper is already App-root fixed layer
- AI currently Expanded/Collapsed/Closed

Product Decisions:
- custom titlebar
- one wallpaper
- AI two-state only
- no X
- no FAB
- whole rail clickable

...
```

---

# 115. ENVIRONMENT Update

After automated gate:

```text
Active Development:
DEV-0065.1

Desktop Shell:
Custom Titlebar implemented

Native Decorations:
OFF

Higher AI:
Expanded / Collapsed only

Schema:
v024

Migration:
0

Backend Domain Changes:
0

AI Runtime Changes:
0

Dependency Changes:
0

Automated Gate:
PASSED

Human Runtime:
PENDING
```

Do not mark Human Runtime PASS until user tests real window controls.

---

# 116. STOP Conditions

## STOP-01 · SOURCE_CONFLICT

Current source no longer matches dynamic main-window architecture.

---

## STOP-02 · UNKNOWN_WORKTREE_DIFF

Unexpected source diff outside known worktree.

---

## STOP-03 · TAURI_CAPABILITY_CONFLICT

Exact required permission IDs do not validate against current generated Tauri schema.

---

## STOP-04 · SECOND_WINDOW_REQUIRED

Implementation appears to require defining a second `main` window in tauri.conf.

Do not do it.

---

## STOP-05 · WINDOW_TRANSPARENCY_REQUIRED

Someone proposes `transparent(true)` to show wallpaper.

Do not do it.

---

## STOP-06 · AI_RUNTIME_CHANGE_REQUIRED

Two-state rail supposedly requires modifying AI runtime, conversations, provider, grounding, ChangeSet, or backend.

Do not do it.

---

## STOP-07 · DB_MIGRATION_REQUIRED

No migration allowed.

---

## STOP-08 · DEPENDENCY_REQUIRED

No new dependency allowed without explicit decision.

---

## STOP-09 · RESPONSIVE_REDESIGN_REQUIRED

Narrow breakpoint requires redesign beyond current shell.

---

## STOP-10 · WINDOW_RESIZE_RUNTIME

Borderless window cannot resize normally in real Windows Human Runtime.

Report. Do not invent custom grips.

---

## STOP-11 · TITLEBAR_DOUBLE_CLICK_RUNTIME

Native drag-region double click does not maximize/restore.

Report before adding another path.

---

# 117. Definition of Done · Automated

All must be true:

```text
Window created dynamically in lib.rs
tauri.conf windows remains []

decorations(false) present
transparent(true) absent

exact four window permissions added

DesktopTitlebar at App root
titlebar height = 34
same global wallpaper continues behind titlebar
no titlebar wallpaper image

app content offset once at shell level
layout uses 100% height
profile gate uses 100%

AI Closed state removed
AI X removed
AI FAB removed
AI open/setOpen UI shell state removed
ui.ai_panel_open no longer consumed by AiPanel UI

AI mode only:
expanded / collapsed

missing AI mode defaults collapsed
whole 46px rail clickable

AI runtime event strings preserved
aiStartRun preserved
aiCancelRun preserved
ChangeSetReview preserved
Enter/Shift+Enter preserved

no API/types/domain-page change
no AI backend change
no migration
no dependency

tsc PASS
npm build PASS
cargo check PASS

batch0651_ui PASS
batch064_ui PASS
batch064r2_ui PASS
batch063_ui PASS
ai_panel PASS
AI regression suites PASS
```

---

# 118. Definition of Done · Human

Required real Windows Human Runtime:

```text
persistent white Windows titlebar gone
custom Higher titlebar visible
wallpaper reaches top

drag works
double-click maximize works
minimize works
maximize works
restore works
close works
resize works
taskbar respected

AI X absent
AI FAB absent
AI collapse works
entire rail expands
conversation preserved
mode persists
page navigation stable
AI send works
Proposal still works
```

---

# 119. Final Trae Response Format

Use exactly this structure:

```text
DEV-0065.1 AUTOMATED GATE RESULT

Status:
AUTOMATED GATE PASSED · HUMAN RUNTIME PENDING
/ STOP <CODE>

Timestamp Source:
SYSTEM

Baseline:
HEAD:
Branch:

Known Worktree Before:
...

Desktop Window Source:
Rust WebviewWindowBuilder / FAIL

tauri.conf windows:
[] / FAIL

Native Decorations:
OFF / FAIL

Transparent OS Window:
NO / FAIL

Window Permissions:
allow-close:
allow-minimize:
allow-toggle-maximize:
allow-start-dragging:

DesktopTitlebar:
Created / FAIL

Titlebar Height:
34px / FAIL

Global Wallpaper Consumers:
production count:
titlebar image consumer: 0 / FAIL

App Shell Offset:
PASS / FAIL

AI Mode:
Expanded + Collapsed only / FAIL

AI Close X:
REMOVED / FAIL

AI FAB:
REMOVED / FAIL

Whole Rail Click:
IMPLEMENTED / FAIL

Legacy ui.ai_panel_open Frontend Consumption:
REMOVED / FAIL

AI Runtime Changes:
0 / FAIL

Backend Domain Changes:
0 / FAIL

Schema:
v024

Migration:
0

Dependencies:
0

Frozen Product Files Modified:
...

Frontend:
tsc:
build:

Rust:
cargo check:

Tests:
batch0651_ui:
batch064_ui:
batch064r2_ui:
batch063_ui:
ai_panel:
batch062r1:
batch062r:
batch062:
batch061r:
batch0602:

Known Deferred P2:
Planning week selected-date detail:
Old Conversation no_changeset anomaly:

Human Runtime:
PENDING

Files Added:
...

Files Modified:
...

TRAE_RUN:
Updated

ENVIRONMENT:
Updated

Git Commit:
NO

Git Push:
NO
```

Then STOP.

Do not begin DEV-0065.2.

---

# 120. Final Product Principle

After DEV-0065.1, Higher should feel like one coherent desktop application:

```text
┌──────────────────────────────────────────────────┐
│ Higher                              —   □   ×    │
│        same wallpaper continues underneath       │
├────────────┬──────────────────────────┬───────────┤
│ Sidebar    │ Main                     │ Higher AI │
│ translucent│ translucent surfaces     │ / Rail    │
│            │                          │           │
└────────────┴──────────────────────────┴───────────┘
```

Not:

```text
Windows white bar
+
Higher WebView
```

And not:

```text
AI Expanded
AI Collapsed
AI Closed
AI FAB
```

The final shell truth is:

```text
ONE APP
ONE WALLPAPER
ONE CUSTOM TITLEBAR
TWO AI PANEL STATES
```
