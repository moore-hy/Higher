# DEV-0065.3 · Higher v1.0 Release Cleanup & Repository Convergence

## 0. Roles

- User = Product Owner
- ChatGPT = Product / Architecture Decision Owner
- Trae = Implementation Engineer
- Trae must not redesign product behavior.
- `.higher/TASK.md` is read-only to Trae.
- This DEV is a cleanup/convergence task, not a feature task.

---

# 1. Baseline

Expected:

```text
repo: C:\Users\37653\Desktop\Higher
branch: main
HEAD: c8e1172c3700a198471c9617994936909115c8f6
worktree: clean
```

Preflight:

```powershell
cd "C:\Users\37653\Desktop\Higher"
git rev-parse HEAD
git branch --show-current
git status --short
```

If mismatch:

```text
STOP
BASELINE_MISMATCH
```

Never use:

```text
git reset --hard
git clean -fd
git restore .
git checkout .
```

---

# 2. Goal

Convert the current working Higher repository into a clean long-term development repository before the v1.0.0 freeze.

Target principles:

```text
SOURCE = retained
LIVE DEV TOOLING = retained
REPRODUCIBLE BUILD OUTPUT = removable
TEST PERSONAL DATA = removable
HISTORICAL PROJECT CONTEXT = archived, not mixed with current context
PRODUCTION USER DATA = never touched
```

This DEV must not change Higher product behavior.

---

# 3. Production Data Safety Boundary

ABSOLUTELY DO NOT TOUCH:

```text
%LOCALAPPDATA%\com.higher.desktop\
```

including:

```text
higher.db
attachments\
vault\
backups\
EBWebView / WebView storage
```

This cleanup operates only inside:

```text
C:\Users\37653\Desktop\Higher
```

Installed Higher data is outside project cleanup scope.

---

# 4. KEEP — Active Development Source

Must retain:

```text
src\
src-tauri\src\
src-tauri\tests\
src-tauri\capabilities\
src-tauri\icons\
src-tauri\migrations via src-tauri\src\migrations\
public\
branding\
scripts\
```

Must retain:

```text
package.json
package-lock.json
tsconfig.json
vite.config.ts
index.html
.gitignore

src-tauri\Cargo.toml
src-tauri\Cargo.lock
src-tauri\build.rs
src-tauri\tauri.conf.json
src-tauri\.gitignore
```

Do not remove current icon assets.

---

# 5. KEEP — Node Modules for Immediate Local Development

Local:

```text
node_modules\
```

may remain.

Reason:

```text
user explicitly wants current real-time development environment ready to launch
```

It remains ignored by Git.

Do not add it to Git.

---

# 6. DELETE — Reproducible Build Output

Delete locally:

```text
dist\
release\
src-tauri\target\
src-tauri\gen\
```

These are generated/reproducible.

Expected approximate reclaim from audit:

```text
dist                 ~3 MB
release              ~211 MB
src-tauri\target     ~24.8 GB
src-tauri\gen        <1 MB
```

`src-tauri\target` deletion will make the next Rust build slower once. This is accepted.

Do not remove corresponding `.gitignore` rules.

---

# 7. DELETE — Disposable Development/Test Runtime Data

User explicitly declared current development/test data disposable.

Delete:

```text
src-tauri\.data\
src-tauri\.webview-data\
```

Audit showed approximately:

```text
.data          ~42 MB
.webview-data  ~513 MB
```

This intentionally resets the next `tauri dev` run to a fresh development database and fresh development WebView state.

Do not migrate this data.

Do not copy it to production.

---

# 8. KEEP — Browser Reproduction Mock

Keep:

```text
public\mock\inject.js
```

and current `index.html` mock loader.

Reason:

```text
it remains part of the browser reproduction/dev workflow
and real Tauri runtime self-no-ops via __TAURI_INTERNALS__
```

No mock redesign in this DEV.

---

# 9. Root Convenience Launcher Cleanup

Current tracked root file:

```text
测试启动按钮.bat
```

is a legitimate development convenience but is not suitable as a permanent root artifact because it hardcodes:

```text
C:\Users\37653\Desktop\Higher
```

Move/rename it to:

```text
scripts\Start-Higher-Dev.bat
```

Rewrite it so project root is resolved relative to the script, not hard-coded to Windows username/path.

Desired behavior:

```text
double click
→ locate repository root from %~dp0\..
→ check npm
→ check cargo
→ npm run tauri dev
→ preserve useful error/pause behavior
```

No business behavior changes.

---

# 10. `.higher` Active Context Problem

Audit confirms `.higher/TASK.md` is stale and still contains:

```text
DEV-0062R.1 · Probe Input/Output Truth Repair
```

while current HEAD is already through DEV-0065.2R + app icon.

This must be corrected organizationally before v1.0.

Trae MUST NOT edit `.higher/TASK.md`.

ChatGPT/User will replace the active TASK input after this cleanup.

Trae may report:

```text
ACTIVE_TASK_POINTER_STALE
```

but must not modify it.

---

# 11. `.higher` Top-Level Task Archive

Current top-level historical task documents:

```text
DEV-0059_Higher_Personal_Planning_Truth_OneShot_TASK.md
DEV-0059.1_Truth_Wiring_Human_Path_Closure_TASK.md
DEV-0059.2_Final_Human_Path_Guardrails_TASK.md
DEV-0063_Higher_UI_Redesign_v1_TASK.md
DEV-0064R_Higher_UI_Redesign_v2_Custom_Wallpaper_TASK.md
DEV-0064R.2_Single_Global_Wallpaper_Surface_System_TASK.md
DEV-0065.1_Higher_Desktop_Shell_Custom_Titlebar_AI_Rail_TASK.md
DEV-0065.2R_Higher_Clean_Windows_Release_Persistent_Data_TASK.md
```

Move with `git mv` into:

```text
.higher\archive\tasks\
```

Use flat filenames or grouped DEV folders; do not alter file contents.

Recommended final:

```text
.higher\
├─ PRODUCT.md
├─ WORKING_RULES.md
├─ UI_CONSTITUTION.md
├─ ENVIRONMENT.md
├─ TASK.md
├─ TRAE_RUN.md
├─ progress\
└─ archive\
   ├─ audit\
   ├─ history\
   ├─ reference\
   └─ tasks\
```

---

# 12. `.higher/progress` Historical Duplicate

Current:

```text
.higher\progress\CURRENT.md
.higher\progress\CURRENT-pre-DEV-0057.md
```

`CURRENT-pre-DEV-0057.md` is a historical snapshot and should not stay in active progress.

Move it to:

```text
.higher\archive\history\progress\CURRENT-pre-DEV-0057.md
```

Keep:

```text
.higher\progress\CURRENT.md
```

---

# 13. TRAE_RUN Convergence

Current `.higher/TRAE_RUN.md` has grown to ~142 KB / ~1009 lines and contains many prior DEV run histories.

Preserve history before trimming:

```text
git mv is NOT appropriate because active TRAE_RUN must remain.
```

Instead:

1. copy existing contents unchanged to:

```text
.higher\archive\history\reports\TRAE_RUN-through-DEV-0065.2R.md
```

2. replace active `.higher/TRAE_RUN.md` with a concise current-run record for DEV-0065.3.

Historical archive must be byte/content preserved before replacement.

---

# 14. ENVIRONMENT Convergence

Do not rewrite ENVIRONMENT from scratch.

Review current `.higher/ENVIRONMENT.md` and fix only stale current-state truths introduced by completed release/icon work.

Must update at least:

```text
current app icon is no longer P2 legacy yellow/blue icon
current Higher icon = committed white/ivory H icon
current HEAD baseline = c8e1172...
release cleanup status
```

Fix stale current-state wording that still says production path is generic `app_data_dir` where the current truth is:

```text
app_local_data_dir
%LOCALAPPDATA%\com.higher.desktop
```

Historical archive files may retain their historical wording.

Do not rewrite historical evidence.

---

# 15. PRODUCT / WORKING_RULES / UI_CONSTITUTION

Keep.

No product semantic changes.

No policy changes unless required solely to describe repository cleanup.

---

# 16. Frontend Dead-Code Findings — INVESTIGATE ONLY

Static import reachability audit found these current frontend files are not reachable from `src/main.tsx`:

```text
src/pages/Progress.tsx
src/pages/Review.tsx

src/components/Donut.tsx
src/components/NoteView.tsx
src/components/ProfileCalendar.tsx

src/components/LearningDataPanel.tsx
src/components/LearningEditor.tsx
```

Evidence:

```text
/progress currently redirects to /planning
/review currently redirects to /planning?date=...
Donut + ProfileCalendar are only used by Progress
NoteView is only used by Review
LearningDataPanel and LearningEditor have no source importers
```

DO NOT DELETE THESE FILES IN DEV-0065.3.

Reason:

```text
removing source code + related CSS/API/backend surface is code convergence,
not filesystem garbage cleanup.
```

Record them in:

```text
.higher/ENVIRONMENT.md
Known Technical Debt / Dead UI Candidates
```

Future task may remove them with separate full regression.

---

# 17. Rust / Migration / Tests

KEEP ALL:

```text
src-tauri/src/migrations/v001...v024
src-tauri/tests/**
src-tauri/src/repository/**
src-tauri/src/ai/**
```

No historical test pruning in this DEV.

Tests are regression assets, not garbage.

---

# 18. Icon Assets

KEEP:

```text
branding\higher-icon.png
src-tauri\icons/**
```

Even generated Android/iOS/icns variants remain tracked for now.

Reason:

```text
small (~3 MB total)
stable release asset pack
avoids accidental Tauri config/bundle breakage
```

No image regeneration in this DEV.

---

# 19. Current 0.3.0 Installer

`release\Higher_0.3.0_Setup.exe` is an ignored build artifact.

Delete it with `release\`.

The installed application remains unaffected.

The v1.0.0 installer will later be regenerated from source.

---

# 20. Git Ignore

Keep current ignore semantics for:

```text
node_modules/
dist/
src-tauri/target/
release/
*.db
src-tauri/.data/
src-tauri/.webview-data/
.env*
```

Ensure `src-tauri/gen/` is ignored via src-tauri/.gitignore as already intended.

Do not ignore:

```text
branding/
src-tauri/icons/
scripts/
.higher/
```

---

# 21. No Version Bump Yet

Do NOT change:

```text
0.3.0 → 1.0.0
```

in this cleanup DEV.

The version bump happens only after:

```text
cleanup
→ dev runtime check
→ regression gate
→ release freeze
```

---

# 22. No License Work

Do not add:

```text
ActivationGate
license
device binding
crypto
DRM
server
offline license
```

in this DEV.

---

# 23. No Domain / AI Changes

Forbidden product behavior changes:

```text
Goals
Tasks
Planning
Knowledge
Sessions
Profiles
AI Runtime
Provider
SemanticAction
Grounding
ChangeSet
Approval/Apply
Wallpaper
Desktop Shell
AI Rail
```

---

# 24. Schema Freeze

```text
schema = v024
migration delta = 0
```

---

# 25. Dependency Freeze

```text
npm dependency delta = 0
Rust dependency delta = 0
```

---

# 26. Cleanup Execution Order

Required order:

```text
A. baseline verify
B. inventory paths
C. archive `.higher` historical active-root docs
D. converge TRAE_RUN / ENVIRONMENT
E. move/fix dev launcher
F. remove generated build/runtime folders
G. verify Git diff
H. restore dev runtime from source
I. regression tests
J. report
```

---

# 27. Safe Removal Commands

Trae may remove ONLY these exact repository-local generated/runtime paths:

```text
dist
release
src-tauri\target
src-tauri\gen
src-tauri\.data
src-tauri\.webview-data
```

Do not glob broader paths.

Before each delete, resolve and print full path and verify it starts with:

```text
C:\Users\37653\Desktop\Higher\
```

If any resolved path escapes repository:

```text
STOP
DELETE_BOUNDARY_VIOLATION
```

---

# 28. Post-Cleanup Expected Root

Expected visible repository root:

```text
Higher\
├─ .git\
├─ .higher\
├─ branding\
├─ node_modules\
├─ public\
├─ scripts\
├─ src\
├─ src-tauri\
├─ .gitignore
├─ index.html
├─ package.json
├─ package-lock.json
├─ tsconfig.json
└─ vite.config.ts
```

Not expected immediately after cleanup:

```text
dist\
release\
src-tauri\target\
src-tauri\.data\
src-tauri\.webview-data\
src-tauri\gen\
测试启动按钮.bat
```

Note:

```text
dist / target / .data / .webview-data may reappear after build/dev runtime.
That is normal because they are generated and Git-ignored.
```

---

# 29. Development Runtime Verification

After cleanup:

```powershell
cd "C:\Users\37653\Desktop\Higher"
npm run tauri dev
```

Expected:

```text
fresh dev DB created
fresh dev WebView data created
Higher opens
ProfileWelcome clean state is acceptable
custom titlebar works
AI Rail works
navigation works
```

Because `.data` was deliberately reset, old dev test data must NOT return.

Close Higher after smoke.

---

# 30. Automated Gate

After runtime smoke:

```powershell
cd "C:\Users\37653\Desktop\Higher"

npx tsc --noEmit
npm run build

cd src-tauri
cargo check -j 1

$env:RUST_TEST_THREADS='1'

cargo test --test batch0652_release -j 1
cargo test --test batch0651_ui -j 1
cargo test --test batch064r2_ui -j 1
cargo test --test batch064_ui -j 1
cargo test --test batch063_ui -j 1
cargo test --test ai_panel -j 1
cargo test --test batch062r1 -j 1
cargo test --test batch062r -j 1
cargo test --test batch062 -j 1
```

If old tests fail only because this cleanup moved `.higher` historical TASK paths, report exact assertion before changing test.

No test weakening without explicit semantic reason.

---

# 31. Disk Reclaim Report

Record before/after sizes for:

```text
Higher root total
src-tauri\target
src-tauri\.webview-data
src-tauri\.data
release
dist
```

Expected maximum immediate reclaim from audit:

```text
~25 GB
```

Do not promise exact value.

---

# 32. Git Diff Expectations

Allowed tracked changes:

```text
.higher/** organizational moves/current context update
scripts/Start-Higher-Dev.bat
delete root 测试启动按钮.bat (via rename/move)
```

No source product change expected.

If `src/**` or `src-tauri/src/**` changes:

```text
STOP
UNEXPECTED_PRODUCT_DIFF
```

---

# 33. No Commit / Push

Trae must not:

```text
git commit
git push
git tag
create release branch
```

Human Runtime + ChatGPT review first.

---

# 34. STOP Conditions

```text
BASELINE_MISMATCH
DELETE_BOUNDARY_VIOLATION
PRODUCTION_DATA_PATH_TOUCHED
UNEXPECTED_PRODUCT_DIFF
SCHEMA_CHANGED
DEPENDENCY_CHANGED
DEV_RUNTIME_FAILED
REGRESSION_FAILED
UNKNOWN_TRACKED_FILE
```

On STOP: report evidence, do not improvise.

---

# 35. Final Report

```text
DEV-0065.3 CLEANUP RESULT

Status:
AUTOMATED GATE PASSED · HUMAN REVIEW PENDING
/ STOP <CODE>

Baseline:
HEAD:
branch:
worktree before:

Deleted Generated:
dist:
release:
src-tauri/target:
src-tauri/gen:

Deleted Disposable Dev Runtime:
src-tauri/.data:
src-tauri/.webview-data:

Node Modules:
KEPT

Production AppLocalData:
UNTOUCHED

Higher Meta:
historical DEV TASKs archived:
CURRENT-pre-DEV-0057 archived:
TRAE_RUN archived+converged:
ENVIRONMENT current truth updated:
TASK.md modified:
NO

Dev Launcher:
scripts/Start-Higher-Dev.bat:
root old bat removed/renamed:

Dead UI Candidates:
RECORDED ONLY · NOT DELETED

Schema:
v024

Migration:
0

AI Runtime Changes:
0

Domain Changes:
0

Dependencies:
0

Runtime Smoke:
PASS/FAIL

Tests:
tsc:
vite build:
cargo check:
batch0652_release:
batch0651_ui:
batch064r2_ui:
batch064_ui:
batch063_ui:
ai_panel:
batch062r1:
batch062r:
batch062:

Disk:
before:
after:
reclaimed:

Git:
status:
diff --stat:

Files Added:
Files Moved:
Files Modified:
Files Deleted:

Commit:
NO

Push:
NO

Tag:
NO

Next:
Higher v1.0.0 Release Freeze
```

Then STOP.
