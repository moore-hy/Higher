{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "android",
  "description": "Higher Android mobile permissions (core / dialog / notification only)",
  "platforms": ["android"],
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "notification:default",
    "barcode-scanner:default"
  ]
}
