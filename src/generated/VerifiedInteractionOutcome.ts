// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { InteractionOutcome } from "./InteractionOutcome";
import type { VerificationMethod } from "./VerificationMethod";
import type { VerifierOutcome } from "./VerifierOutcome";

/**
 * 受控验证通路的返回值。
 *
 * `verifier` 与 `verification` 一并返回，是为了让调用方（与前端）能诚实呈现
 * 「这次到底验证成功了没有、依据是什么」，而不是自己猜。
 */
export type VerifiedInteractionOutcome = { outcome: InteractionOutcome, 
/**
 * 验证器的真实输出（含 `result` 与真相源指针）。
 */
verifier: VerifierOutcome, 
/**
 * 本次**实际**采用的判定方式（未命中时是 `SelfCheck`）。
 */
verification: VerificationMethod, };
