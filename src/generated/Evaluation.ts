// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.

/**
 * 学习验证记录：一次练习 / 测试 / 回忆 / 应用 / 项目 / 其他的验证结果。
 *
 * 只记录事实（发生了什么验证 + 结果是什么），不解释原因（Feedback System），不做自动调整（Adjustment System）。
 *
 * 关键可空设计：
 * - learning_item_id 可空（全科模拟 / 阶段综合 / 学习方法验证等不绑定到特定节点）
 * - total_items / correct_items / incorrect_items 全部可空（回忆/应用等验证不一定有题数）
 * - score / max_score 全部可空（非打分型验证可不填）
 *
 * evaluation_type 枚举：practice / test / recall / application / other
 * outcome 枚举：unrated / passed / partial / failed
 */
export type Evaluation = { id: number, 
/**
 * v013 起 Profile 直挂
 */
profile_id: number, 
/**
 * v013 起可空（Goal = 可选规划上下文）
 */
goal_id: number | null, learning_item_id: number | null, title: string, evaluation_type: string, source: string | null, occurred_at: string, total_items: number | null, correct_items: number | null, incorrect_items: number | null, score: number | null, max_score: number | null, outcome: string, note: string | null, created_at: string, updated_at: string, 
/**
 * v021 §20：Evidence V1 —— 可选关联真实 StudySession（sessions → evaluations）
 */
session_id: number | null, 
/**
 * v021 §20：user | ai | import
 */
source_kind: string, 
/**
 * v021 §20：来源引用（如 ai_run_id / import 文件路径）
 */
source_ref: string, 
/**
 * v021 §20：trusted | needs_review（needs_review 不得进入 trusted evidence）
 */
trust_state: string, };
