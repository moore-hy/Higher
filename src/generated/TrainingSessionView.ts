// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { BlockCompletionState } from "./BlockCompletionState";
import type { TrainingBlockRun } from "./TrainingBlockRun";
import type { TrainingInteraction } from "./TrainingInteraction";
import type { TrainingRun } from "./TrainingRun";

/**
 * 一次训练的**完整**持久化视图。
 *
 * 一次 IPC 返回整页所需（run + 块 + 已发生的交互 + 每个块的完成契约状态）：
 * 前端不得为每个块各开一个命令，否则「后端唯一真相」会被拆成一堆局部读取。
 *
 * `completions` 是 D19 的落点：完成契约与「现在能不能往下走」由后端算好一起返回，
 * 前端只负责把**已经写死的冻结规则**讲给用户听，不自己判定。
 */
export type TrainingSessionView = { run: TrainingRun, blocks: Array<TrainingBlockRun>, interactions: Array<TrainingInteraction>, completions: Array<BlockCompletionState>, };
